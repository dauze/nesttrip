# NestTrip — Roadmap

Ce document sert de référence pour le projet : ce qui est déjà en place (à ne pas casser) et ce qu'il reste à faire.

## 📋 Plan d'exécution en cours (branche `claude/roadmap-planning-r5e7av`)

Objectif : traiter tout ce qui n'est pas marqué "non prioritaire" ci-dessous. Décisions prises avec l'utilisateur le 2026-07-28 :

- **Vue calendrier** (UI Desktop) : reportée, pas assez spécifiée pour l'instant.
- **Compteur de somme devise** : reporté (un onglet dédié n'est pas voulu, le mettre dans le header prendrait trop de place — à retravailler plus tard).
- **Devise par défaut** : préremplissage des nouvelles saisies uniquement, jamais rétroactif sur les activités/réservations déjà créées.
- **Tri des réservations** : 100% automatique (chronologique, passées en bas) ; le glisser-déposer manuel a été retiré.

Décisions prises avec l'utilisateur le 2026-07-30 :
- **Titre Vol/Train (Logistique)** : passe en texte simple en lecture seule (calculé par la cinématique guidée) + crayon dédié pour l'écraser manuellement, plutôt qu'un champ éditable en première zone de saisie.
- **Ville de retour d'un vol** : pas de traitement spécifique — un aller-retour reste 2 éléments "Vol" distincts créés normalement, annotation retirée du "🔧 À faire".

Items **mis en pause** (structurants, pas assez cadrés pour être lancés sans nouvelle discussion — voir section Qualité/process) : empaquetage mobile (Capacitor ?), secret de déploiement (nécessite une valeur fournie par l'utilisateur).

Décisions prises avec l'utilisateur le 2026-07-31 (section "UX / Interactions") :
- **Cinématique carte du pool Général** : décorrélation totale du scroll (contrairement à la vue jour, inchangée) — tour lent point à point sur 2s d'inactivité, mise en pause (sans jamais forcer la caméra) sur toute action directement sur la carte, reprise du tour là où il en était après 2s. Détail complet dans "✅ Déjà fait".
  - **Correctif (2026-07-31)** : le retour à la vue d'ensemble sur toute action "ailleurs sur la page" (scroll du pool, clic sur une carte d'activité, recherche, tri...) a été retiré — retour utilisateur : ce comportement initial gênait, la cinématique ne doit se mettre en pause QUE sur une action directement sur la carte, jamais ailleurs (`GeneralMapCinematicService` : suppression de `onPageAction`/`onRootPointerDown` et des écouteurs scroll/`pointerdown` associés).
- **Bouton "+"** : le même menu à 7 entrées (Activité + 5 types Logistique + Note) s'ouvre désormais partout, jour ET onglet Général — l'ancienne création directe contextuelle du "+" sur Général est retirée.
  - **Précision (2026-08-02)** : revu — ce menu à 7 entrées reste finalement réservé au contexte jour. Dans le pool général (Activités/Logistique/Listes), le "+" est redevenu spécifique à l'écran affiché. Détail dans "✅ Déjà fait".

Ordre d'exécution : la section "UX / Interactions" (tout ce qui n'est pas "non prioritaire") a été traitée intégralement le 2026-08-02, puis la section "Activités" (tout ce qui n'est pas "non prioritaire") le même jour — voir "✅ Déjà fait" pour le détail des deux lots. Un nouveau lot de puces ajouté depuis à la section "UX / Interactions" a été traité intégralement le 2026-08-04 (9 des 10 items non "non prioritaire" — le 10e, l'accroche onboarding, mis de côté ce jour-là, voir "🔧 À faire") — voir "✅ Déjà fait".

En cours (démarré le 2026-08-04) : section "### UI", fusionnée avec l'étude de direction artistique `DESIGN.md` en une seule grosse livraison visuelle (design system + toolbar + carte + activity card/timeline + mobile-trip-nav + FAB/app-message + graphique Résumé), livrée par phases (un commit logique par phase, `nesttrip-verify` entre chaque). Décisions actées avec l'utilisateur avant implémentation :
- **Icône engrenage (toolbar)** : rotation douce de l'icône pendant que le menu réglages s'ouvre/se ferme (la puce roadmap originale s'arrêtait à "pendant que le menu sort" sans suite), + une vraie animation d'apparition pour le popup desktop (actuellement aucune, contrairement au tiroir mobile).
- **Persistance du toggle carte repliée/dépliée** : vraie préférence utilisateur Firestore (`users/{uid}`, nouveau champ `mapCollapsedByDefault`), pas du localStorage — seule exception logique de ce lot au scope "visuel uniquement" de `DESIGN.md`. `firestore.rules` interdisait jusqu'ici toute écriture client sur `users/{uid}` (champ géré par Cloud Functions) ; la règle est assouplie pour n'autoriser le client à modifier QUE ce champ précis de son propre doc.
- **Carte Google** : retrait des contrôles de navigation par défaut (zoom/compass — pinch/scroll restent), et affinement du style des marqueurs déjà thémés (halo primary sur le marqueur sélectionné).
- **Graphique "types d'activité" (Résumé)** : anneaux concentriques (un par catégorie, longueur d'arc ∝ proportion, légende dessous) plutôt qu'un donut classique — référence visuelle `public/graphisme.png` (fournie par l'utilisateur, remplace `public/graphique.html` qui s'est avéré être une page Freepik morte sans visuel exploitable) ; top 5 combiné activités + transport + logement, couleurs = tokens `--nt-activity-*`/`--nt-logistic-*` déjà existants.

Intercalé le 2026-08-05, sur demande explicite de l'utilisateur : la section "Bugs / fixes" (14/15 items traités, un reste ouvert) + 4 demandes complémentaires hors roadmap — voir "✅ Déjà fait" pour le détail. Le lot "### UI" ci-dessus reste le prochain à reprendre.

Tout ce qui a déjà été livré (avec le détail des correctifs) est listé dans **✅ Déjà fait**, tout en bas.

## 🔧 À faire

### Régressions post-migration OnPush/PrimeFlex/Angular 22 — reste à confirmer par l'utilisateur

Le gros du lot remonté le 2026-07-29 après la clôture des 4 items OnPush/PrimeFlex/Angular 22 (commit `c2e2ee7`), y compris le gel total au clic sur un jour en mode mobile (antérieur à la migration, voir "Déjà fait"), est corrigé et détaillé dans "✅ Déjà fait". Reste :
- Check-in/check-out (réservation) : aucune régression fonctionnelle identifiée dans le code pour "le clavier ne sort plus" — seule cause plausible trouvée et corrigée, le même rétrécissement de largeur que la zone de dates du jour (voir "Déjà fait"). À reconfirmer une fois le correctif de largeur testé ; si le problème persiste, redécrire précisément ce qui se passe (clavier qui ne s'affiche pas du tout ? champ qui refuse la saisie ?).
- Chaînage réservation bloqué à l'heure de début quand elle est saisie en tapant heure puis minute sur le cadran (sans passer par OK) : reproduit via Playwright (clics réels sur le cadran, sans OK) et le chaînage continue bien jusqu'à la date de fin dans ce test — pas confirmé cassé dans le code actuel. Possible que ce soit lié à l'ancien défaut "heure actuelle" du cadran (corrigé, voir "Déjà fait") ou à une nuance tactile réelle (mobile) non reproduite par un clic simulé. À retester maintenant que le cadran démarre à 00:00 ; si ça bloque encore, préciser à quel geste exact (drag vs tap, 1er ou 2e chiffre) ça coince.

### Offline & données (non prioritaire)

- Mode hors ligne : quid des données Google (Maps/Places) en offline ?
- Stockage des fichiers en local si possible

### UI spécifique Desktop

- Vue calendrier (reporté, pas assez cadré)
- Améliorer la vue jour, le résumé de la journé est trop étiré là
- Le scroll auto sur le premier element fait que l'on ne peut pas rester en haut en vu desktop, pas cool 
- Le drag and drop 
- refondre toute la partie générale

### UI 
- De manière générale, aggrandi les border radius : Il faut qu'ils soient tous pilotté par un token css qui est dans token scss si ce n'est pas le cas.
- TOOLBAR : 
  - Supprime les borders
  - Supprime les margins et padding top, left et right
  - Met un box shadow au bottom, qui ne doit pas se voir lorsque l'on est sur un daypanel puisque la carte est sencé être en continuité
  - Qunad on clique sur la roue cranté, pendant que le menu sort 
- Carte : modifier son mode de fonctionnement sur la vue Days : 
  - Elle doit etre sticky à la tooltip header, donc elle peut sortir du day swiper : ca simplifiera la gestion au slide et la transposition qui ne sera plus nescessaire
  - La navigation avec le scroll sur les activités reste inchangé
  - il faut rajouter un mode pour le panel togglelable, Il y aura pas de header, mais un footer qui contiendra au centre le logo "pi pi-map" et en dessous de lui, centré également  un "pi pi-minus" de primeng : le clique sur un de ces boutons permettra de l'ouvrir et de la fermer
  - Un box shadow sera sur le padding bottom, comme actuellement, et il n'y aura pas de border radius sur les 2 coté du haut nide de padding à gauiche et à droite  pour qu'elle s'incruste dans la toolbar 
  - Lorsque l'on est en haute de l'écran tout en haut du scroll, La vue d'ensemble de la journée doit être juste après de la carte, qu'elle soit ouverte ou fermée : cela veut dire qu'il faut donc calculer le padding au début du day swoper en fonction de la hauteur de la map (qui varie entre ouvert et fermé)
  - Stocker l'information de la carte toogle ou pas directement sur l'utilisateur : déployée par défaut, comme ça si il la ferme et actualise, elle restera fermée
  - Est ce que tu peux personaliser les boutons de navigation de la carte ? Si oui : 
    - Enlève le bouton de navigationn
    - Fais un meilleur design pour les carte, qui se rapproche plus de mon thème, avec la couleur primary pour l'item selectionné
- Transformer le panel qui contient toutes les activités qui à un header "Activités" par un pannel non togglable sans titre : cela devrait siumplifier le scroll sur une activité car il y a plus bezsoin de verifier qu'il est toggle, attendre potentiellement qu'il soit ouvert, puis scroll
- mobile-trip-nav, il faudrait la refondre : 
  - la bar du bas ne doit plus contenir les notion de "Résumé", "Activité", "Logement & transport" et "Listes" : ceux-ci doivent être déporté dans la liste day-strip comme pour les jour. La bar deviens donc plus simple car elle contient que 2 bouton qui permettent de switcher rapidement de l'un à l'hautre et de toujours afficher "Général" et "Jour n". En revanche, quand on a sélectionné "Général", seul les 4  days trip "Résumé", "Activité", "Logement & transport" et "Listes" doivent être affiché". Au clique sur "Jour n", le comportmeent reste identique, seule les jour sont affiché dans la deuxième bar
  - Esthétiquement, la deuxième bar doit plus etre en overlay mais avoir un fond de la même couleur que la toolbar, et avoir un box shadow pour que l'on ai le sentiment que celle-ci soit posé au dessus au même niveau que la toolbar. Il faudrait également revoir les couleurs (qui fera parti du thème ) dans le mode clair pour un rendu plus naturelle, et dans le mode sombre pluis proche des même teinte que choisi pour le thème  
- Comme la toolbar n'aura plus de border, tu peux réduire de moitier le border de tous les contenu de daytrip  
- Dans résumé dans la tab résumé, mettre un graphique plutôt avec les différents types d'activités, il ressemblera à "public/graphique.html avec les 5 + gros type d'activité/ transport /logement. Il faut que les couleurs de types d'activités soient respectées, et que si il y en a moins de 5 sélectionné, ne pas les afficher
- Les boutons du switch de classement ne semble pas centrées à cause du box-shadow, faire en sorte qu'il paraisse visuellement centré

### Carte

- Gérer le cas Asie : alternative à Google Maps (non prioritaire)
- Rajouter la Position actuelle de l'utilisateur sur la carte (non prioritaire)

### Activités

- Suggestions d'activités via la ville dans le pool (non prioritaire)
- Calcul auto des trajets entre activités (à pied / voiture / vélo) (non prioritaire — visuel de référence à challenger si le calcul temps réel s'avère trop lent : `public/distance entre activités.png`) (non prioritaire)

### Nouveau voyage / IA (non prioritaire)

- Page "nouveau voyage" : appel IA pour pré-remplir jours/activités/période en fonction des choses à faire, si l'utilisateur propose des trucs, dis ce qu'il veut faire, excetera
- Proposer une amélioration d'itinéraire par jour. Je ne sais pas comment le matérialiser, mais ça permettrait de modifier l'ordre des activité, en prenant compte les horaires d'ouverture et les distances (IA)

### I18n (non prioritaire)

- Variabiliser tous les libellés de l'application dans un fichier de propriété
- faire renaming de tout pour avoir un truc stylé : exemple "Nouvelle aventure" plutôt que "créer un voyage" 
- Internationalisation de l'app (textes)

### Collaborateurs (non prioritaire)

- Email quand ajouté à un trip

### Devise (non prioritaire)

- Gestion de la devise par défaut qui sera affiché à l'utilisateur en fonction d'ou il vient, rajouter pays d'origine dans la demande d'inscription ? Puis modificable via un paramétrage stocké au niveau de l'utilisateur.
- Gérer la devise sélectionnée automatiquement dans les activité / réservations en fonction du lieu de la destination. C'est cette devise qui sera modifiable dan l'onglet "infos"
- Faire évoluer la carte dans l'onglet résumé via un clique sur le montant pour afficher la liste des dépenses dans une popup qui contient un tableau de toutes les dépenses, le tableau aurait 3 colonnes : montant, libellé et date. La dernière ligne contient un bouton + qui permet de rajouter une ligne de dépense ou l'utilisateur est invité à mettre le montant via la devise, le libellé, et la date (initialisée à la date du jour) via un chainage. On peut cliquer pour mopdifier les dépenses rajoutées à la main mais les dépenses associées aux activités sont grisées. même système que pour le reste pour la supression des montant, clique prolongé et multiselection possible.
- il faut également faire un chantier pour que le montant total affiché dans l'onglet info affiche le montant convertie avec la devise de l'utilisateur et la devise du voyage en petit.
- Gérer toutes les conversions de devises 

### UX / Interactions
- Parcours nouveau client, arriver directement sur le champ "Destination" (renommé, voir "✅ Déjà fait") fait bizarre : il faudrait une vraie accroche/salutation avant. Mis de côté (2026-08-04) — le dialog mobile "Destination" s'ouvre immédiatement via la cinématique guidée (`NewTripComponent.startGuidedEntry`/`afterNextRender`), donc un texte posé sur l'écran de fond ne serait jamais vu ; un vrai onboarding demande une réflexion à part (nouvel écran ? étape dédiée avant le chaînage ?), pas juste une phrase ajoutée au-dessus d'un champ.
- Créer le mode avec l'aide pour la premiere fois qu'on utilise l'application : des popup qui expliquent comment faire (non prioritaire)
- Rajouter les types de transport préférer en fonction des distances des trajets pour le calcul des distances sur le résumé du voyage, dans trip header en dessous des dates (non prioritaire)
  - Les transport doivent être "à pied si temsp en dessous de .. min, velo entre tant et tant de minutes, train sinon. Il faudrait pouvoir donner des intervalles et des modes de transport préféré, pour le calcul des distances. Il faut Remarque : faire un truc simple à utiliser, mais paramétrable si on veut, j'ai besoin de ton aide pour bien faire et surtout avoir une valeur par defaut facile  (non prioritaire)
- Prérenseigner une liste de to take à la création d'un voyage (non prioritaire)
- Rajouter des attributions sur tout pour pouvoir mettres des trajets, hotel et des transports + mettre une note "si le transport et partagé, mettre le prix unitaire' (non prioritaire)
  - Cela serait par defaut assigné à tous les voyageurs mais on pourrait en enlever
  - Dans le calcul du prix, compter que ceux ou le voyageur est sur les trajets et les activités
- Rajouter filtre mon planning et celui de tout le l'équipe (non prioritaire)
- Rajouter les transports / hotel des notification directement dans la vu d'ensemble (non prioritaire)
- mettre les badges des compagnons de route en haut, et mettre des chip en detail dans le résumé, cliquable aussi (non prioritaire)

### Bugs / fixes
- A l'ajout d'un train, la cinématique de remplissage ne fonctionne pas — **investigué en profondeur le 2026-08-05** (comparaison octet par octet avec les 4 autres cinématiques guidées qui fonctionnent, chaîne complète `DayLogisticQuickAddService` → `LogisticFocusService` → `LogisticsListComponent` → `LogisticCardComponent` → `guidedTrain()`, `PlaceAutocompleteFieldComponent`, filtrage Google Places backend, historique git) sans cause racine certaine trouvée, aucun correctif appliqué pour ne pas risquer un correctif hasardeux. Piste ouverte non confirmée : `guidedTrain()` est la seule cinématique actuellement exercée qui alterne un dialog CDK `Dialog` (recherche de gare, `openTitleDialog`) ENTRE deux CDK `Overlay` (`guidedDateTime`) — une interaction focus/z-index plausible mais non vérifiable sans navigateur réel (pas d'identifiants `.env.e2e` dans ce sandbox). À reproduire sur un vrai appareil/session avec accès Playwright authentifié pour aller plus loin.

### Qualité / process

- Améliorer le .ico (manifest + png) : depuis un téléphone, "exporter comme application" (PWA) génère une icône floue. Il faut un vrai jeu d'icônes + manifest. Mis de côté pour l'instant, le logo pouvant encore changer.
- Tests e2e (Playwright) : socle en place le 2026-07-28 (compte de test Firebase Auth dédié, `npm run e2e`) — parcours 1 (login) et 2 (création de trip) couverts, parcours 3 à 7 (activité, form jour, dispatch, réservation, suppression) en backlog, voir `.claude/skills/nesttrip-e2e/SKILL.md`.
- empacter le tout dasn une application pour mobile ? Comment gérer la cohabitation ? — **en pause** : décision d'architecture (Capacitor ? store ?) à prendre avec l'utilisateur avant de commencer, pas lancé dans ce lot.
- Il faudrait faire des dossier pour les composants dans shared, il y a trop d'élément à plat là
- CSS : j'ai pas l'impression que tout utilise les variable et que tout soit bien variabilité : par exemple il y a des 0.5rem et des 0.25rem. Et pour le mode desktop vs mobile, il devrait y avoir un attibut global qui est allimenté soit par 0.25 si mobile, soit 0.5 si desktop et utilisé partout non c'est pas possible ? Ce ne serait pas plus simple ?
- La selection de fichier : le composant est duppliqué entre le pool d'activité et les activités à tord 

## ✅ Déjà fait
- **Section "Bugs / fixes" + 4 demandes complémentaires, 2026-08-05** : 14 des 15 items de la section (le 15e, cinématique train, reste ouvert — voir "🔧 À faire") + les 4 demandes explicites de l'utilisateur en plus de la section.
  - **Renommage "Etat résa" → "Statut"** : libellé du champ statut de réservation dans `ActivityFormComponent` (`#bookingSelect`, `booking-status`).
  - **Padding gauche/droite du day swiper disparu** : `--nt-space-1` (0.25rem, essai de réduction du lot "### UI") sur `.tab-content-wrapper` remis à `--nt-space-2` (0.5rem, valeur historique reproduisant le `ml-2`/`mr-2` de `.app-content`) — retour utilisateur explicite, le padding avait complètement disparu visuellement. Couvre aussi la partie gauche/droite de l'item ROADMAP "le padding à droite n'est pas bon" ; la partie "padding bottom 2x trop gros" du même item n'a pas été retouchée (elle réserve l'espace de la barre de nav du bas, pas comparable à un inset de bord — aucune régression identifiée, et non redemandée explicitement dans ce lot).
    - **Correctif (2026-08-05)** : retour utilisateur — `--nt-space-2` (0.5rem) était en fait 2x trop large. Remis à `--nt-space-1` (0.25rem, la valeur retirée par erreur au départ n'était donc pas le bon repère de comparaison).
    - **Incohérence révélée sur accueil-trip/new-trip (2026-08-05)** : une fois trip-detail à `--nt-space-1`, retour utilisateur — accueil-trip/new-trip paraissaient maintenant "trop paddés" par comparaison. Cause : `body` ne réinitialisait QUE `margin-top` (0.3rem), laissant les 8px de marge par défaut du navigateur à droite/gauche/en bas — invisibles sur trip-detail (contenu `position: fixed`, ignore la marge de `body`) mais bien réels sur accueil-trip/new-trip/login (flux normal). `body` reçoit désormais un `margin` complet (`0.3rem 0 0`) et `.app-content` (`trips.component.html`, wrapper d'accueil-trip/new-trip) un `padding-left/right: var(--nt-space-1)` explicite, pour matcher exactement trip-detail.
  - **Map ne se ferme pas en quittant un jour pour un élément logistique** : `.day-fixed-map` (conteneur `position:fixed` mobile où la carte partagée est déplacée en contexte 'day') n'était jamais reparqué par les onglets Activités/Logistique/Listes (aucun des trois n'appelle `TripDayMapHostService.moveTo`), donc restait affiché par-dessus ces onglets une fois un jour quitté. Nouveau `TripDayMapHostService.park(anchor)` (déplace la carte vers `.map-anchor`, conteneur neutre) + `effect()` dans `TripDaySwiperComponent` qui reparque dès que `activeId()` n'est ni un jour ni "summary".
  - **Horloge (heure de fin d'activité), sélecteur "J+N"** : toujours affiché dès "J+0" (au lieu de n'apparaître qu'au franchissement de minuit détecté automatiquement). Le contrôle "fin < début" ne s'applique plus qu'à J+0 (fin de l'auto-bascule silencieuse vers J+1) : bouton OK grisé + `<app-message severity="error">` sous le cadran si invalide à J+0, cinématique auto (tap heure → tap minute → validation/fermeture) désormais interrompue dans ce cas plutôt que de fermer le dialog sur une valeur invalide (`TimePickerClockComponent.endBeforeStartAtDayZero`, `stopDrag`/`selectValue`).
  - **Cinématique de remplissage à l'ajout d'un train** : investigué en profondeur sans cause racine certaine — **reste ouvert**, voir "🔧 À faire" pour le détail de l'investigation.
  - **Dates de début/fin et titre du trip pas rafraîchis en dynamique à distance** : `TripFacade.mergeFromRemote` ne réécrivait jamais `_trips`/`_days`/`_tripDays` après l'hydratation initiale (seul `hydrate()`, au premier chargement, le faisait) — un jour ajouté/supprimé ou un titre changé par un autre collaborateur ne se répercutait donc jamais localement. Même remplacement complet keyed sur `trip.days` que `_dayActivityIds` (déjà correct) étendu à `_days`/`_tripDays`, et mise à jour de `_trips[tripId]` (titre/ville/devise/lieu/propriétaire) à chaque snapshot distant — `_tripTitle`/`_tripCurrency` (signaux dédiés existants) continuent de shadower toute édition locale en cours, aucun risque de retour en arrière introduit.
    - **Régression corrigée le jour même (2026-08-05)** : ce premier correctif réécrivait `_trips`/`_days`/`_tripDays` avec une NOUVELLE référence à CHAQUE snapshot distant, y compris quand rien n'y avait changé (ex. l'édition d'un simple champ d'activité, qui vit dans le MÊME document Firestore que le trip) — cassait la stabilité de référence dont `activeTrip()` (computed) dépend, donc l'UI optimiste tout entière (tout se réactualisait à chaque frappe débattue sur une activité, retour utilisateur explicite : "je peux plus rien tester"). Ces trois écritures sont désormais CONDITIONNELLES : `_trips[tripId]` n'est réécrit que si titre/ville/propriétaire/devise/lieu ont réellement changé (comparaison champ à champ, même principe que `_tripMembers.update` juste à côté, qui faisait déjà ça) ; `_days`/`_tripDays` ne sont réécrits que si l'ensemble des clés de jour a réellement changé (comparaison par `Set`, insensible à l'ordre) — et dans ce cas, seuls les jours réellement NOUVEAUX reçoivent un nouvel objet `Day`, les jours déjà connus gardent leur référence. Nouveau test `trip-facade.service.spec.ts` (3 cas : snapshot sans changement pertinent → références inchangées ; titre/jour ajouté à distance → bien répercuté ; jour supprimé à distance → bien retiré).
    - **2e régression, même jour, même symptôme** : le correctif ci-dessus ne suffisait toujours pas (retour utilisateur : "tout se réactualise" persistait). Cause plus profonde, présente dans TOUTE la méthode (pas seulement les 3 signaux déjà corrigés) : `_poolActivities`/`_dayActivityInstances`/`_dayActivityIds`/`_tripActivities`/`_logistics`/`_tripLogistics` recevaient CHACUN une nouvelle référence CONTENEUR à chaque snapshot, même quand les valeurs qu'ils contiennent étaient déjà préservées par référence individuellement (les boucles le faisaient déjà bien). Or `TripStore.getDayActivities(dayId)` (et les sélecteurs analogues `getDayActivitiesWithEchoes`/`getDayActivity`/`getDayActivityWithDay`/`getPoolActivityView`/`getAllPoolActivities`/`getAllLogistics`) COMPOSENT un résultat neuf (`composeInstanceView`, `.map()`...) à CHAQUE exécution de leur `computed()`, et ce `computed()` dépend de la RÉFÉRENCE de ces signaux LARGES/PLATS (tous jours/toutes activités confondus, voir "état normalisé" CLAUDE.md) : la moindre nouvelle référence conteneur, même vide de changement réel, le faisait réexécuter ET republier pour TOUS LES JOURS du trip à la moindre confirmation Firestore d'UNE SEULE activité éditée. Corrigé à deux niveaux : 1) toutes les écritures de `mergeFromRemote` sont maintenant conditionnelles (`recordsShallowEqual`/`arraysEqual`, nouveaux helpers) — y compris `_dayActivityIds`, qui en profite pour aussi arrêter d'écraser les entrées des jours des AUTRES trips déjà chargés (bug de portée pré-existant, corrigé au passage) ; 2) chacun des sélecteurs composés ci-dessus reçoit un `equal` custom (`TripStore.structurallyEqual`, comparaison JSON) sur son `computed()` : la fonction continue de se réexécuter (coût CPU minime) mais ne notifie ses CONSOMMATEURS que si le résultat a RÉELLEMENT changé structurellement — la stabilité de référence se propage donc jusqu'au template, ce qu'une simple préservation en amont ne suffisait pas à garantir. Nouveaux tests dans `trip-facade.service.spec.ts` (référence de `getDayActivities(jour)` stable sur un snapshot identique ET quand une activité d'un AUTRE jour est éditée, tout en restant réactive quand SON activité change réellement).
  - **Modification de la date de début depuis Résumé qui mutait le trip à tort** : déjà résolu incidemment par la refonte du header voyage dans Résumé (2026-08-01) — `TripSummaryComponent.onDatesChange` passe déjà par `tripFacade.addDay`/`removeDay` (optimiste), plus aucune trace de l'ancienne mutation directe.
    - **3e régression, titre/dates spécifiquement (2026-08-05)** : malgré les deux correctifs ci-dessus, éditer le TITRE ou l'INTERVALLE DE DATES continuait de tout réactualiser, et un renommage/changement de dates fait par un autre collaborateur ne s'affichait pas en direct. Deux causes distinctes, toutes deux dans `_tripTitle`/`_tripCurrency` (shadows dédiés voulus pour NE PAS toucher `_trips`, voir leur doc) : 1) `updateTripTitle`/`updateTripCurrency` ne touchent QUE le shadow, jamais `_trips[tripId]` — le snapshot de CONFIRMATION qui suit systématiquement l'écriture comparait donc le titre déjà confirmé à l'ANCIEN `_trips[tripId].title` (jamais mis à jour), vu comme "différent" à CHAQUE frappe, réécrivant `_trips` (donc `activeTrip()`) sans raison réelle. 2) une fois `_tripTitle[tripId]` posé par MON édition, il shadowait `_trips[tripId].title` INDÉFINIMENT — un renommage par un AUTRE collaborateur, arrivé plus tard, mettait bien à jour `_trips[tripId].title` en interne mais restait invisible côté `getTripTitle` (qui préfère toujours le shadow). Corrigé : nouveau `TripStore._pendingTripFieldIds` (même principe que `_pendingActivityIds`) protège `trip.id` pendant que l'écriture Firestore est en vol — `mergeFromRemote` ignore alors tout le bloc titre/devise ; une fois l'écriture confirmée (retirée du set), la comparaison se fait contre la valeur EFFECTIVE (`_tripTitle[tripId] ?? _trips[tripId].title`) plutôt que `_trips` seul, et un changement réellement distant efface le shadow en plus de mettre à jour `_trips` — restaure le direct pour tout renommage futur. Plus profondément, `TripHeaderComponent` (formulaire titre + dates) prenait encore `[trip]="tripFacade.activeTrip()"` en entrée (pour lire `trip.days`) : même en l'absence de toute régression `_trips`, ce binding le recouplait à TOUT ce qui fait varier `activeTrip()`, jamais isolé comme le voulait le pattern `_tripTitle`. Nouveau `TripStore.getTripDateRange(tripId)` (sélecteur mémoïsé, `equal` structurel — même famille que `getTripTitle`/`getTripCurrency`, dérivé de `_tripDays`/`_days` mais indépendant de `activeTrip()`) exposé via `TripFacade`, `TripHeaderComponent.dateRange` (nouvel input) remplace `trip` entièrement — le formulaire de dates se resynchronise désormais UNIQUEMENT quand ce signal dédié change réellement, qu'il s'agisse de ma propre édition ou d'un collaborateur distant. Nouveaux tests `trip-facade.service.spec.ts` (stabilité de `getTripDateRange` + confirmation d'un renommage local sans écrire `_trips` + renommage distant appliqué après mon propre renommage confirmé).
    - **4e régression, l'intervalle de dates spécifiquement continuait de tout réactualiser (2026-08-05)** : contrairement au titre (une seule écriture Firestore), `TripSummaryComponent.onDatesChange` déclenche PLUSIEURS `addDay`/`removeDay` (un par jour ajouté/retiré) pour une seule édition d'intervalle — chacun un `Promise` direct sans aucune protection "pending" (le mécanisme `_pendingTripFieldIds` du titre ne couvrait que `updateTripTitle`/`updateTripCurrency`). Un snapshot de confirmation intermédiaire (reflétant seulement UNE partie des écritures encore en vol) faisait alors réécrire `_days`/`_tripDays` plusieurs fois de suite pendant la même édition. Corrigé par un nouveau `TripStore._pendingTripDayIds`, à base de COMPTEUR (`pendingTripDayCounts: Map<tripId, number>`) plutôt qu'un simple Set comme pour le titre : plusieurs `addDay`/`removeDay` peuvent être en vol simultanément pour le même trip, donc la protection ne doit se lever qu'une fois TOUTES les écritures en cours confirmées, pas dès la première. `mergeFromRemote` ignore désormais tout le bloc `_days`/`_tripDays` (y compris le nettoyage "jour retiré" de `_dayActivityIds`) tant que `trip.id` est dans ce set. Nouveau test `trip-facade.service.spec.ts` simulant exactement la course (2 `addDay` en vol, un snapshot intermédiaire arrivant entre les deux confirmations Firestore) : le nombre de jours reste stable jusqu'à la confirmation complète.
    - **5e régression, la VRAIE cause de "l'intervalle de dates rafraîchit toujours toute la page" — pire, affiche les anciennes dates après coup (2026-08-05)** : le correctif `_pendingTripDayIds` ci-dessus protégeait bien `TripStore`/`mergeFromRemote`, mais `TripSummaryComponent.tripDateRange` (et `tripTitle`, même pattern) ne les consultait même pas dans ce cas : les deux préféraient INCONDITIONNELLEMENT `tripFacade.trips().find(t => t.id === id)` (`fromList`) au signal dédié `getTripDateRange`/`getTripTitle` dès que `fromList` avait une valeur. Or `trips()` est alimenté par `TripDataSource.getTrips$()` — une requête Firestore sur toute la COLLECTION `trips` (pour l'accueil), une souscription `onSnapshot` entièrement SÉPARÉE de celle du trip actif (`getTrip$`), qui recalcule `earliestDay`/`latestDay` directement depuis les clés brutes du champ `days` du document à CHAQUE snapshot — sans passer par `_pendingTripDayIds`, ni par aucune protection. Toute la protection ajoutée dans `mergeFromRemote` (régression précédente) était donc court-circuitée dès que cette souscription non protégée livrait un snapshot pendant la fenêtre d'édition, qui pouvait refléter un état intermédiaire OU encore l'ancien intervalle — d'où le symptôme aggravé "après modification, ce sont les anciennes dates qui sont affichées" (le mécanisme de protection existait bien, mais sur le mauvais signal). Corrigé en inversant la priorité : nouveau `TripFacade.hasTrip(tripId)` (passthrough vers `TripStore.hasTrip`) permet à `tripDateRange`/`tripTitle` de préférer le signal dédié protégé dès que le trip est hydraté dans le store, `fromList` ne servant plus que pour le tout premier rendu (avant que `loadTrip`/`getTrip$` n'ait résolu). Nouveau test `trip-facade.service.spec.ts` (`hasTrip`) ; pas de test de composant pour `TripSummaryComponent` lui-même (coût de setup élevé pour ce composant, voir skill `nesttrip-testing`).
    - **6e régression, la VRAIE VRAIE cause du skeleton affiché à chaque édition de dates (locale OU distante), 2026-08-05** : malgré les 5 correctifs ci-dessus, retour utilisateur — la modification du range de dates affiche toujours le skeleton de chargement, et au retour (une fois le skeleton reparti) les dates n'avaient toujours pas l'air d'avoir changé, alors que la liste des jours du swiper, elle, était déjà correcte. Cause distincte des 5 précédentes, dans `TripDetailComponent` cette fois (pas `TripFacade`/`TripStore`) : l'`effect()` qui réinitialise `contentReady` (donc affiche `<app-trip-detail-skeleton class="skeleton-overlay">`, voir `trip-detail.component.html`) lisait `this.facade.activeTrip()?.id` mais se redéclenchait pour TOUTE nouvelle référence de `activeTrip()` — `activeTrip()` (computed) recompose un nouvel objet `Trip` à chaque fois que `_tripDays`/`_days` changent, y compris pour le MÊME trip quand un jour est ajouté/supprimé (comportement voulu à ce niveau, voir CLAUDE.md), donc à CHAQUE édition d'intervalle de dates, locale OU distante. Contrairement à l'effect d'initialisation du jour actif juste au-dessus (qui, lui, gardait déjà un `initializedTripId` pour ne s'exécuter qu'une fois par trip), celui-ci n'avait aucun garde-fou équivalent : il remettait `contentReady` à `false` (skeleton affiché) et redémarrait le filet de sécurité de 4s à CHAQUE fois, masquant jusqu'à 4 secondes un contenu déjà à jour — d'où l'impression trompeuse "les dates n'ont pas changé" une fois le skeleton reparti (ce n'était qu'un voile temporaire par-dessus un contenu déjà correct, jamais une vraie régression de données à ce stade). Corrigé en ajoutant `contentReadyTripId` (même principe que `initializedTripId`) : le skeleton ne se redéclenche plus que lors d'un VRAI changement de trip. Comme cet effect se base sur `activeTrip()?.id` pour TOUT changement de jours (pas seulement local), ce même correctif couvre aussi la 2e exigence formulée par l'utilisateur ("qu'elles changent sans skeletons si quelqu'un les modifient dans la bdd") sans code supplémentaire. Nouveau test e2e Playwright `e2e/trip-date-range.spec.ts` (pas exécutable dans ce sandbox — aucun `.env.e2e`/compte de test Firebase configuré ici, voir `.env.e2e.example` — mais prêt à tourner avec de vrais identifiants : crée un trip, ouvre Résumé, étend l'intervalle de dates, vérifie que `.skeleton-overlay` n'apparaît jamais et que le libellé affiche bien le nouvel intervalle).
    - **7e régression, dernière : le skeleton ne s'affiche plus, mais le libellé "Date : ..." de Résumé ne changeait toujours pas après sélection (2026-08-05)** : bug distinct des 6 précédents (ceux-ci vivaient tous dans `TripFacade`/`TripStore`/`TripDetailComponent` ; celui-ci dans `TripHeaderComponent` lui-même). `dateRangeLabel` était un `computed(() => { const dates = this.dateForm.value.dates; ... })` — `FormGroup.value` est un getter ORDINAIRE, pas un signal : un `computed()` ne suit que les LECTURES DE SIGNAUX pour savoir quand se réévaluer, donc ce `computed()` ne traçait AUCUNE dépendance et se figeait DÉFINITIVEMENT après sa toute première évaluation — ni une sélection utilisateur (qui patch `dateForm`), ni `patchFromRange` (déclenché par `dateRange()`, lui bien un signal, à chaque changement local confirmé ou distant) ne le faisait plus jamais réévaluer. Corrigé en dérivant `dateRangeLabel` directement de `dateRange()` (le signal d'entrée, déjà la source de vérité que `patchFromRange` recopie dans le form) plutôt que du form : le chaînage `onDatesSelected` → `datesChange` → `TripFacade.addDay`/`removeDay` → `_tripDays`/`_days` étant synchrone, `dateRange()` reflète la nouvelle plage sans latence perceptible, y compris pour ma propre sélection. Nouveaux tests `trip-header.component.spec.ts` (premier spec de composant du repo avec du vrai rendu de dépendances de template — `ViewportService` factice fournie pour éviter `window.matchMedia`, non polyfillé dans l'environnement de test Vitest, voir skill `nesttrip-testing`).
  - **Sélecteur "J+N" de l'horloge : positionner explicitement "J+1" (sans faire franchir minuit à l'heure choisie) ne créait ni écho sur le jour suivant ni chip sur l'activité (2026-08-05)** : `TimePickerDialogComponent.openDialog()` émettait `dayOffsetChange` (l'offset explicite choisi via les flèches du dialog) AVANT `closed` (la nouvelle heure de fin). Or `closed` déclenche côté `ActivityFormComponent` un recalcul (`recalculateFrom('endTime')` → `syncEndDayOffsetFromTimes`) qui réévalue `endDayOffset` à partir des heures seules et le remet à 0 dès que l'heure de fin choisie n'est pas *littéralement* antérieure à l'heure de début — ce qui écrasait silencieusement tout offset "J+1"/"J+2" explicitement positionné par l'utilisateur si son heure de fin n'était pas, en plus, avant minuit au sens naïf (cas pourtant explicitement voulu par la demande initiale : "le contrôle... doit seulement être fait à J+0", n'importe quelle heure étant valide au-delà). Corrigé en inversant l'ordre d'émission (`closed` puis `dayOffsetChange`) : le recalcul automatique s'exécute d'abord, l'offset explicite de l'utilisateur est appliqué en dernier et gagne toujours. Nouveaux tests `time-picker-dialog.component.spec.ts`.
  - **Drag-and-drop depuis le pool général qui redirige à tort sur le jour** : `ActivityDayDispatchOverlayComponent` appelait `DayActivityFocusService.requestFocus` après TOUT dispatch, y compris `origin === 'pool'` — restreint à `origin === 'day'` (déplacement inter-jours) ; le dépôt depuis le pool général crée sa nouvelle instance sans plus jamais quitter le pool.
  - **Scroll vers l'élément logistique fraîchement créé/ouvert qui ne fonctionne pas** : `LogisticsListComponent.focusCardWhenReady` appelait `scrollIntoView` dans la même passe synchrone que `collapsed.set(false)`, mesurant donc encore la géométrie "carte repliée" — même correctif que `DayReorderService` pour le même symptôme (2 `requestAnimationFrame` avant de mesurer/scroller).
  - **Écart 2x trop gros entre les tuiles Dépenses/Résumé** : déjà corrigé (`gap-2` → `gap-1` sur `.trip-summary-tiles`, vérifié via `git log`).
  - **Titre d'une liste (onglet Listes) en largeur max plutôt qu'au contenu** : nouveau `.nt-content-sized-field` (`field-sizing: content`, `utilities.scss`) sur le textarea titre, `flex-1` retiré — combiné à `.nt-capped-inline-field` déjà en place pour plafonner un titre très long.
  - **Valeurs du formulaire d'activité en `width:100%` plutôt qu'au contenu** : `w-full` → `w-max` sur les selects Type/Statut et sur `app-price-field`, même pattern déjà en place sur le champ Deadline du même formulaire.
    - **Correctif (2026-08-05)** : retour utilisateur — "Début", "Durée" et "Note" étaient toujours trop larges (oubliés du premier passage, à tort supposés déjà content-sized faute de règle `width` explicite — en réalité stretchés par le `align-items:stretch` par défaut du conteneur flex-colonne, même mécanisme que les champs déjà corrigés). `w-max` ajouté sur les deux `app-time-picker-dialog` (Début/Durée — Fin non concerné, déjà dans une ligne flex qui ne stretch pas la largeur). Pour Notes, correctif ciblé DANS `NotesFieldComponent` plutôt qu'au niveau de l'appelant : seul `.notes-field-trigger` (mobile) passe en largeur au contenu plafonnée (`width: max-content; max-width: 100%`, conserve le clamp 2 lignes pour un texte long) — le `<textarea class="w-full">` desktop reste volontairement pleine largeur (un textarea multi-lignes a besoin de place pour taper).
  - **Crayon nom du trip (Résumé), focus + clavier mobile** : vérifié structurellement identique aux autres dialogs titre qui fonctionnent déjà dans l'app (`autoFocus` CDK sur le sélecteur du champ + `ngAfterViewInit` `focus()`/`select()`, `viewContainerRef` bien transmis) — aucune divergence de code trouvée, aucun changement appliqué. Ouverture du clavier virtuel mobile non re-vérifiable en conditions réelles dans ce sandbox (pas d'appareil mobile réel) — **à tester**.
  - **Bouton retour applicatif qui ne ramène pas systématiquement à l'accueil** : déjà correct — `TripsComponent.goBack()` fait `router.navigate(['/trips'])` sans aucune condition sur le jour/onglet actif, entièrement découplé du popstate navigateur (`bindPopState`/`Location.go`, rewrite du 2026-08-01).
  - **"Suite de ..." d'une activité (écho) pas entouré** : `.activity-echo-card` gagne le hairline standard (`--nt-content-border-width`/`--nt-content-border-color`, même token que `PanelComponent`/`CardComponent`) à la place de `border: none`.
  - **Double-clic sur "Jour n" (calendrier mobile)** : vérifié, `(dblclick)="openDayCalendar()"` et `DayPickerSheetComponent` toujours fonctionnels — aucune régression trouvée (fiabilité du double-tap tactile natif non vérifiable sans appareil mobile réel).
  - **Crayon à côté de la devise (Résumé)** : nouveau bouton crayon (même gabarit que `.trip-header__pencil`/`.logistic-header__pencil`) à côté du select devise — celui-ci masque déjà son chevron (`no-chevron`, pour rester discret à côté du montant), le crayon redevient donc le seul indice visuel explicite qu'il est éditable ; ouvre le panneau via `SelectComponent.openPanel()` (`#currencySelect`).
  - **Flag "carte ouverte/fermée par défaut" jamais appliqué** : `TripDayMapComponent` repropageait vers `GoogleMapPanelService.setCollapse()` dès son tout premier rendu (valeur par défaut `false` du `linkedSignal`, lue AVANT que la préférence Firestore de l'utilisateur — asynchrone — n'ait eu le temps d'arriver) — ce premier appel marquait `GoogleMapPanelService.seeded` à tort, bloquant définitivement l'application de la vraie préférence une fois chargée. Le tout premier passage de cet effect de repropagation est désormais ignoré, quel que soit le contexte.
    - **Correctif définitif (2026-08-05)** : le flag "premier passage ignoré" restait fragile (retour utilisateur : "je ferme la carte, je recharge, elle est de nouveau ouverte" — toujours cassé). Root cause structurelle plutôt que contournée : l'`effect()` générique réagissait à TOUT changement de `collapsed()`, y compris ceux causés par la préférence Firestore qui se charge de façon externe, pas seulement un vrai clic utilisateur — aucun flag ne peut distinguer ça de façon fiable. Remplacé entièrement par un hook sur `(afterToggle)` de `PanelComponent` (émis UNIQUEMENT par `PanelComponent.toggle()`, donc uniquement sur un vrai clic du footer) : `onCollapsedToggled()` ne repropage plus jamais qu'en réaction à un geste réel, éliminant la classe de course entière plutôt que de la contourner par un flag. Nouveau `google-map-panel.service.spec.ts` (préférence appliquée une fois chargée après le premier rendu ; pas écrasée par une préférence différente arrivant après une bascule manuelle ; `toggleCollapse` persiste bien).
  - Vérifié : `ng lint` (0 erreur), `npx tsc --noEmit -p tsconfig.app.json` (0 erreur), `ng test` (59/59). Vérification visuelle réelle non effectuée (pas d'identifiants de test Firebase `.env.e2e` configurés dans ce sandbox) — items marqués "à tester" ci-dessus à confirmer par l'utilisateur.
- **4 correctifs supplémentaires sur la refonte UI, 2026-08-05** :
  - **Grabber du footer de la carte trop petit** : `.map-panel__grabber` doublé en LONGUEUR uniquement (`width` : 2rem → 4rem, `trip-day-map.component.scss`) — un premier essai doublait aussi l'épaisseur (`height`), corrigé suite au retour utilisateur (l'épaisseur ne doit pas bouger).
  - **Skeleton de la carte visible pendant le swipe entre jours** : `DayPanelComponent` affichait un `<app-skeleton>` dans `.sticky-map` tant que `!activeMapComponent()` — un état qui ne devrait plus jamais se produire en pratique (la carte partagée est montée une seule fois, très tôt, par `TripDaySwiperComponent`) mais qui pouvait quand même clignoter derrière/devant la vraie carte pendant la transition de swipe entre deux jours. Supprimé entièrement (retour utilisateur explicite : "tu peux le supprimer complètement") plutôt que patché — `.sticky-map` reste un `<div>` vide en layout empilé (déjà masqué par `.sticky-map:empty { display: none; }`, voir le correctif de la veille), et redevient le conteneur réel de la carte en layout scindé. `GoogleMapPanelService`/`collapsed` (devenus inutilisés dans ce composant) retirés avec.
  - **"Vue d'ensemble de la journée" absente si transport sans activité** : `day-panel-body`/`day-panel-map-group` étaient gatés sur `activities().length > 0`, ignorant les jours n'ayant QUE de la logistique (check-in/check-out, prise en charge...) sans aucune activité — passés à `timelineEntries().length > 0` (inclut déjà activités + occurrences logistiques "frontière", voir `getMergedDayTimeline`), donc la timeline (et la carte) s'affichent désormais dès qu'il y a au moins une activité OU un élément logistique ce jour-là.
  - **Montant des dépenses (Résumé) pas centré et trop petit** : `.trip-summary-total` (`trip-summary.component.scss`) gagne `text-align: center` et un `font-size` agrandi (1.75rem → 2.25rem). Complété par un retour utilisateur : le montant doit aussi être centré par rapport à la HAUTEUR RESTANTE de la tuile (celle-ci est étirée par `align-items:stretch` pour matcher sa voisine "Résumé", plus haute — le montant restait collé juste sous "Devise :" sans profiter de l'espace en dessous). Nouveau `CardComponent.fillHeight` (même intention que `PanelComponent.fillHeight`, mais SANS `height:100%` sur le host — son parent direct ici, `.trip-summary-tiles`, n'a pas de hauteur définie, un pourcentage y créerait une circularité qui casserait l'étirement déjà correct ; `display:flex` seul suffit, le host garde la hauteur RÉELLE que `stretch` lui donne) : la ligne "Devise :" garde sa taille naturelle, le montant vit dans un conteneur `flex-1` centré (horizontalement ET verticalement) qui absorbe tout l'espace restant.
  - Vérifié en conditions réelles (Playwright, mesures `getBoundingClientRect`, captures d'écran lues) sur le compte de test dédié, à l'exception du point "transport sans activité" (aucun jour du trip de test n'a actuellement de la logistique sans aucune activité pour le reproduire tel quel — changement de condition simple et à faible risque, revu par lecture de code).
- **6 correctifs remontés en vérifiant la refonte UI, 2026-08-04** :
  - **Carte pas collée à la toolbar / trop d'espace avant "Vue d'ensemble"** : deux gaps redondants s'additionnaient en layout empilé — `.day-panel-root` réutilisait `--chrome-top-offset` (toolbar + gap-1, pensé pour un contenu SANS carte) pour calculer son `padding-top`, alors que `.day-fixed-map` (la carte réelle) est déjà collée à `--chrome-toolbar-height` SEUL, sans ce gap-1 ; ET le placeholder `.sticky-map` local (skeleton avant montage, vide une fois la carte partagée déplacée dans `.day-fixed-map`) participait quand même au `gap` flex de `.day-panel-body`, doublant l'écart. Nouveau `--day-map-chrome-offset` (calculé dans `TripDaySwiperComponent`, `--chrome-toolbar-height` si la carte fixe est active sinon `--chrome-top-offset` inchangé en layout scindé) + `.sticky-map:empty { display: none; }` : l'écart carte -> "Vue d'ensemble de la journée" est passé de ~11.5px à un unique `--nt-layout-gap-stacked` (~3.75px), vérifié par mesure de style calculé (Playwright) en mode replié ET déplié.
  - **Ancien contenu de la map affiché au changement de jour** : reproduit précisément (Playwright, screenshots) — un jour sans AUCUNE activité géolocalisée gardait la caméra (centre/zoom) du dernier jour affiché (aucun marker résiduel, la liste de points était bien vidée, mais rien ne recentrait la caméra). `TripDayMapComponent` : l'effect de suivi de `points()` avait un early-return total sur liste vide (`if (!pts.length) return`) hérité d'un correctif précédent qui empêchait tout recentrage intempestif pendant un scroll/tri — ce early-return empêchait aussi le SEUL cas où un recentrage est nécessaire (aucun système dédié, `DayScrollSyncService`/`GeneralMapCinematicService`, n'a de point vers lequel viser). Nouveau : sur liste vide, recentrage explicite (`moveCameraTo`) vers la même vue par défaut qu'au tout premier chargement.
  - **Padding gauche insuffisant dans les activity card** : le bord gauche en dégradé (`::before`, 0.4rem) mangeait la quasi-totalité du padding uniforme (0.5rem) du contenu, ne laissant qu'0.1rem de respiration après la barre contre 0.5rem sur les 3 autres côtés. Nouvelle custom property `--nt-panel-content-padding-left` sur `PanelComponent` (fallback = padding uniforme habituel, inerte pour tout autre appelant), posée à 0.9rem (barre + 0.5rem) sur `.trip` (`ActivityCardComponent`) uniquement.
  - **Logement/Transport : vrais panels par type** (voir correctif du même jour plus bas — un premier essai avec un header custom "identique visuellement" a été rejeté par l'utilisateur, qui voulait le composant `<app-panel>` littéral malgré le risque).
  - **Bandeau logistique (Check-in/Check-out, Nuit sur place...) avant "Vue d'ensemble de la journée"** : ordre flex de `.day-panel-body` corrigé (`day-panel-banner` passe de `order:1` à `order:3`, après la timeline) — n'affecte que le layout empilé (mobile), les deux vivent dans des colonnes séparées en layout scindé.
  - Les 5 premiers points ont été vérifiés en conditions réelles (Playwright, captures d'écran lues, mesures `getBoundingClientRect`/`getComputedStyle`) sur le compte de test dédié — accès réseau Google Maps/Places fonctionnel dans le bac à sable ce jour-là.
- **Suite retours utilisateur sur la refonte UI, même jour (2026-08-04)** :
  - **Ombre de la toolbar visible par-dessus la carte** : `.app-toolbar` (z-index 20) restait au-dessus de `.day-fixed-map` (z-index 15, `--z-fixed-map`) — même à 0px d'écart géométrique entre les deux, le `box-shadow` de la toolbar (qui peint AU-DELÀ de sa propre boîte) se dessinait par-dessus le haut de la carte, lu comme un filet d'ombre flottant. `--z-fixed-map` passe à la même valeur que `--z-app-toolbar` (`tokens.scss`) : à égalité de z-index, l'ordre DOM tranche (la carte, plus tardive dans le DOM que la toolbar, voir `trips.component.html`), donc la carte peint désormais par-dessus l'ombre plutôt que l'inverse. Un discret hairline de séparation carte/toolbar est repris en plus (`.map-panel`, `trip-day-map.component.scss` — l'ancien `border-top: none` retiré, le hairline bas déjà présent est simplement aussi gardé en haut), demande explicite de l'utilisateur une fois l'ombre corrigée.
  - **Groupe de jour vide (0 activité) déplié par défaut dans le pool, vue Chronologie** : `chronoDayGroups` garde volontairement les jours sans activité (pour une vraie vue d'ensemble du voyage), mais leur `<app-panel>` avait `[collapsed]="false"` en dur comme les jours avec du contenu — rien à montrer, pas de raison de les ouvrir d'office. `[collapsed]="group.activities.length === 0"` (`trip-activities.component.html`) : un jour vide démarre replié, l'utilisateur garde la main pour le déplier.
  - **Pas de padding entre le bord gauche (épais) et le logo dans les cartes logistique** : même correctif que les activity card ci-dessus, appliqué à `.reservation` (`logistic-card.component.scss`) — `--nt-panel-content-padding-left: 0.9rem`.
  - **Logement/Transport, vrais `<app-panel>` par type (reversal du correctif précédent)** : retour utilisateur explicite — le rendu "identique visuellement" (header custom + chevron répliqué) n'était pas acceptable, l'utilisateur voulait le composant `<app-panel>` littéral, comme les groupes ville/jour du pool d'activités. Reproduit et confirmé via Playwright que le risque documenté (régression du 2026-07-30) redevenait bien réel avec des `@for` imbriqués par type : créer un Vol via le "+" flottant (type encore vide dans le trip) fermait le dialog "Numéro de vol" tout seul ~300ms après ouverture, sans interaction, la suite de la cinématique guidée (Résa, dates, prix) sautée entièrement. Root cause réelle, pas juste le symptôme : `LogisticsCreationService`/`triggerCreate` créait TOUJOURS l'élément en type `'other'` D'ABORD, puis la cinématique guidée lui faisait changer de type EN PREMIER — un changement de type post-création vers une section encore vide fait déplacer l'élément d'une boucle `@for` à une autre, qu'Angular traite comme détruire-et-recréer (le tracking ne dédoublonne qu'au sein d'une même boucle), coupant tout dialog CDK ouvert sur l'ancienne instance. Corrigé à la racine (suggestion de l'utilisateur) plutôt que contourné : le "+" flottant du tab Logistique ouvre désormais un menu "Type" AVANT toute création (`TripDetailComponent.logisticsAddMenuItems`/`onFabActivate`, nouveau `<app-menu #logisticsAddMenu>`) — même mécanique que le menu "Ajouter" d'un jour (`DayLogisticQuickAddService.create`, déjà conçu pour accepter un appel depuis le tab Logistique lui-même, `dayDate` absent). Le type est donc TOUJOURS connu avant la création de l'entité Firestore : l'élément naît directement dans la bonne section, plus aucun changement de type après coup, donc plus aucun risque de destruction/recréation pendant la cinématique guidée. `LogisticsCreationService` (devenu mort) supprimé entièrement ; `LogisticCardComponent`/`LogisticDetailsComponent.startGuidedEntry` perdent leur paramètre `skipTypeStep` (toujours vrai désormais, l'étape "Type" de la cinématique guidée n'a plus lieu d'être) ; `LogisticsListComponent.typeGroups` redevient une vraie liste de groupes (`<app-panel [thickBorder]="true">` par type, boucle `@for` imbriquée, markup identique au pool d'activités) ; recherche du tab Logistique déplacée dans `TripCreationTargetService.logisticsSearchTerm` (le menu "Type" vit maintenant dans `TripDetailComponent`, hors de `LogisticsListComponent`, et doit pouvoir lire/vider la même recherche pour le préremplissage du titre). Revérifié en conditions réelles (Playwright) : créer un Vol via le nouveau menu "Type" ouvre directement le dialog "Numéro de vol" et le garde ouvert, aucune carte détruite entre-temps.
  - Vérifié en conditions réelles (Playwright, captures d'écran lues, mesures de style calculé) sur le compte de test dédié.
- **Section "UX / Interactions", nouveau lot traité intégralement, 2026-08-04** : 9 des 10 items non "non prioritaire" de ce lot (le 10e, l'accroche onboarding, mis de côté — voir "🔧 À faire").
  - **Renommage "Destination"** : champ "Ville / Pays" → "Destination" dans `NewTripComponent` (label desktop + titre du dialog mobile).
  - **Renommage "Etat résa"** : libellé "Résa" → "Etat résa" dans `ActivityFormComponent` uniquement (item scopé explicitement à l'activity form, pas touché côté `LogisticDetailsComponent`).
  - **Fusion day-logistic-entry vol/train même jour** : `getLogisticDayOccurrences` perdait silencieusement l'heure d'arrivée d'un vol/train arrivant le même jour que le départ (`isEnd && !isStart` jamais vrai si `isStart===isEnd`) — nouveau champ optionnel `LogisticDayOccurrence.endTime`/`endRole` : une seule occurrence fusionnée porte désormais les deux horaires (`DayLogisticEntryComponent` affiche "HH:mm → HH:mm"), le cas multi-jours (deux entrées séparées) reste inchangé.
    - **Correctif (2026-08-04)** : retour utilisateur — le libellé de rôle ("Départ") n'a plus lieu d'être une fois les deux horaires affichés côte à côte, il induisait en erreur. `DayLogisticEntryComponent` masque désormais ce libellé dès que `endTime` est présent (cas fusionné uniquement).
  - **Recherche + "+" (préremplissage/vidage)** : `TripActivitiesComponent` (pool) préremplissait déjà le titre depuis la recherche, il ne restait qu'à vider `searchTerm` après création (`TripActivitiesCreationConfig.clearSearch`). `LogisticsCreationService.startCreation`/`createLogistic` gagnent un `initialTitle` posé directement sur le `Logistic` créé (visible immédiatement, y compris desktop où le form est inline) ; `LogisticDetailsComponent.guidedLogement`/`guidedOther` (les deux seules étapes "titre libre" de la cinématique guidée — les autres champs `openTitleDialog` sont des lieux/aéroports/gares, hors scope) prefillent leur dialog depuis `this.logistic().title` plutôt qu'une chaîne vide. `NotesComponent.addItem` utilise `searchTerm()` comme titre initial (pas de dialog ni d'étape de confirmation ici, contrairement à Activités/Logistique) puis vide la recherche immédiatement.
  - **Bouton "+" flottant, évitement de bord** : nouveau `FabBottomProximityDirective` (réutilise `getScrollContainer`, déjà utilisé par `DayScrollSyncService`) posé sur le conteneur racine des 3 écrans à cartes (jour, pool d'activités, logistique — pas Listes, hors scope de la condition "carte activité/transport/logement") ; détecte la proximité du bas du scroll et remonte l'info via un nouveau `TripCreationTargetService.avoidEdge`/`setAvoidEdge`, lu par `TripDetailComponent` et passé en `[avoidEdge]` à `FloatingAddButtonComponent`. Le bouton s'écrase sur le bord droit (`transform: translateX + scaleX`, icône contre-décalée pour rester visuellement centrée) et joue un léger rebond élastique (`fab--returning`, `@keyframes fab-return`) au retour — approximation volontaire de l'« aller-retour dû à la vitesse de scroll » demandé : pas de vraie mesure de vélocité (complexité disproportionnée), juste un overshoot fixe à chaque retour.
    - **Correctif (2026-08-04)** : retour utilisateur — le bouton ne se collait pas assez au bord. `:host(.fab-host--edge)` écrase désormais aussi `right` (plus de marge de 2.5rem) en plus du `translateX`/`scaleX` du bouton lui-même, pour un vrai "à moitié avalé par le bord" plutôt qu'un simple rapprochement.
  - **Couleur par type d'activité** (remplace la couleur par statut de réservation) : `ActivityTypeMeta` gagne un `colorVar` (même pattern que `LogisticTypeMeta`), 10 nouveaux tokens `--nt-activity-*` (`tokens.scss`, réutilise les 4 couleurs de statut désormais libres + 6 nouvelles, distinctes des 5 couleurs `--nt-logistic-*`). `ActivityCardComponent` : le bord gauche (`.trip`) est désormais toujours coloré par type (plus de classe `.booking`/`bookingMeta` conditionnelle sur la carte — `hideBookingMeta` ne pilote plus que la visibilité du trombone, jamais la couleur) ; les cartes "à assigner" gardent un bord gauche plein coloré, seuls les 3 autres côtés restent en pointillé. `ActivityFormComponent` : la classe `.booking` colorée se déplace du champ "Etat résa" vers le champ "Type" (`select.component.scss` généralisé en commentaire, mécanisme CSS inchangé). `TimelineComponent` : icônes d'activité, d'écho ET de logistique colorées par type (`ACTIVITY_TYPE_META`/`LOGISTIC_TYPE_META`, remplace l'ancien `nt-text-primary` fixe), l'icône logistique devient aussi celle du type au lieu d'un `pi-ticket` générique — cohérence avec `DayLogisticEntryComponent`, déjà ainsi.
  - **Fin d'activité sur plusieurs jours (J+1, J+2...)** : nouveau champ optionnel `DayActivityInstance.endDayOffset`/`Activity.endDayOffset` (0/absent = même jour) — `resolveEndDayOffset` (`activity-time.util.ts`) résout la valeur effective, rétro-compatible (absent + fin < début ⇒ 1, l'ancienne règle implicite, jamais plus). `TripStore.getDayActivitiesWithEchoes` généralisé : boucle sur tous les jours précédents dans l'intervalle couvert par `endDayOffset`, écho SANS `endTime` sur les jours intermédiaires (pas encore le dernier jour), écho AVEC `endTime` réel sur le dernier.
    - **Correctif (2026-08-04) — repositionnement du sélecteur** : retour utilisateur — les flèches "J+N" doivent vivre DANS le dialog "Heure de fin" mobile (`TimePickerClockComponent`), pas dans le form. `TimePickerClockData` gagne `dayOffsetReference`/`dayOffset` (boîte mutable partagée, évite de changer le type de retour du dialog, toujours `Date | undefined`) ; le sélecteur apparaît à gauche du cadran HH:MM dès que la sélection en cours franchit minuit, avec une colonne toujours réservée (grille `1fr auto 1fr`, jamais togglée) pour ne jamais faire sauter la largeur du dialog à l'apparition/disparition. Défaut à J+1 dès le franchissement (pas J+0, corrigé — `effectiveDayOffset`), flèche gauche bloquée à 1 (tremblement). `ActivityFormComponent` garde un badge "J+N" purement informatif (pas de flèches), cliquable pour rouvrir ce même dialog (`openEndTimeEditor`).
    - **Bug trouvé et corrigé en vérifiant ce lot (2026-08-04)** : les échos au-delà de J+1 ne s'affichaient sur AUCUN jour (ni intermédiaire ni final), reproduit en conditions réelles (Playwright, cinématique mobile complète, trip 4 jours). Cause racine : `tripFromFb` (`trip.mapper.ts`) construisait `trip.days` via `Object.entries(data.days)` sans trier — un champ `map` Firestore ne garantit PAS l'ordre de ses clés au retour (contrairement à l'ordre d'écriture), l'ordre restait chronologique juste après création puis se mélangeait dès la resynchronisation suivant une édition. `TripStore.getDayActivitiesWithEchoes` (comme le mécanisme d'écho simple qui existait déjà avant ce lot) repère "i jours avant" par POSITION dans `_tripDays[tripId]`, en présumant cet ordre chronologique — un tri par clé (epoch ms) manquant à la source cassait donc silencieusement les deux mécanismes. Corrigé en triant `Object.entries(data.days)` par clé numérique croissante dans `tripFromFb`, seul point de vérité (pas de contournement dans `TripStore`).
- **Section "Activités" traitée intégralement, 2026-08-02** : les 4 items non "non prioritaire" restants de cette section, décisions actées avec l'utilisateur avant implémentation.
  - **Widget horaire simplifié** : `DayActivityInstance.startTime`/`endTime` (et la vue composée `Activity`) passent de `Date` à une chaîne `"HH:mm"` pure — la partie date était déjà synthétique (forcée à la date du jour par `ActivityFormComponent.applyDayIdDate`), ce changement ne modifie donc rien côté utilisateur. `day-activity-instance.mapper.ts` reste rétro-compatible en lecture avec l'ancien format (epoch-ms stringifié) indéfiniment, sans script de migration — chaque instance s'auto-met à jour au prochain edit, seule l'écriture n'émet plus que `"HH:mm"`. Nouveau `activity-time.util.ts` (`timeToMinutes`) centralise les comparaisons horaires pour les items suivants.
  - **Affichage multi-jours ("écho lié")** : une activité dont la fin est antérieure à son début (plage franchissant minuit) affiche désormais un écho en lecture seule sur le jour suivant (`TripStore.getDayActivitiesWithEchoes`, nouveau type `ActivityEcho`) — dérivé à l'affichage uniquement, jamais une vraie `DayActivityInstance` ni stocké en base ; le modèle "1 instance = 1 jour" reste inchangé. Le tap sur l'écho (`ActivityEchoCardComponent`) navigue vers l'instance réelle sur son jour d'origine (`DayActivityFocusService`, mécanisme déjà existant).
  - **Placement chronologique automatique + comportement du drag** : `TripStore.updateDayActivityInstance` replace désormais l'activité éditée au bon endroit chronologique dès qu'un horaire de début est renseigné (`insertChronologically`, nouveau `day-activity-order.util.ts`) — les activités sans horaire ne sont jamais retriées entre elles, seules celles déjà datées contraignent le point d'insertion ; le drag manuel d'une activité non datée reste inchangé (persistance via `reorderActivities`, jamais concerné par ce repositionnement). Une nouvelle activité créée via le bouton "+" (`TripStore.createActivity`) est désormais insérée en tête de journée plutôt qu'en fin (le drag depuis le pool général continue lui d'ajouter en fin) — le scroll+focus automatique vers la carte tout juste créée existait déjà et n'a nécessité aucun câblage supplémentaire.
  - **Fusion visuelle timeline jour + logistique** (décision : fusion d'affichage uniquement, les écrans de création/édition Activité et Logistique restent strictement indépendants) : les occurrences logistiques "frontière" (Check-in/Check-out, Départ/Arrivée, Prise en charge/Restitution, Début/Fin) rejoignent désormais la timeline chronologique du jour (`mergeDayTimeline`, nouveau `day-timeline-merge.ts`), aussi bien dans la liste principale (nouveau `DayLogisticEntryComponent`) que dans le mini "Vue d'ensemble de la journée" (`TimelineComponent`, désormais cliquable pour scroller dans le jour via un nouveau `DayScrollSyncService.focusLogisticEntry`, plutôt que de naviguer vers l'onglet Logistique). Les occurrences "continuation" (Nuit sur place / En cours, activité déjà en cours sans horaire actionnable ce jour-là) restent épinglées en haut du jour (`DayLogisticBannerComponent`, inchangé pour elles uniquement). Le tap sur une entrée logistique fusionnée continue de naviguer vers l'onglet Logistique pour éditer (comportement inchangé) ; sa poignée offre en plus un geste de saisie/relâchement autonome (résistance élastique + rebond), volontairement SANS intégrer la mécanique de `DayReorderService` (spécifique aux cartes d'activité et à un réordonnancement réellement persistable) — une occurrence logistique n'a structurellement aucune position à sauvegarder, sa place réelle est toujours recalculée depuis son horaire côté Logistique.
- **Section "UX / Interactions" traitée intégralement, 2026-08-02** : tous les items non "non prioritaire" restants (17 items), sur demande explicite de l'utilisateur.
  - **Logement & Transport** : libellé du tab "Logistique" renommé (id technique `logistics` inchangé, seul l'affichage change — même principe que le renommage "Notes" → "Listes").
  - **Gares/aéroports (Train/Vol)** : côté train, "Ville de départ/arrivée" → "Gare de départ/arrivée" (4 occurrences : champs manuels `TrainFieldsComponent` + dialogs de la cinématique guidée `LogisticDetailsComponent.guidedTrain`). Côté vol, `FlightLookupApiService`/AeroDataBox ne fournit qu'un NOM d'aéroport (ni placeId ni coordonnées) — nouveau `GooglePlaceService.searchOnce$` (recherche ponctuelle, pas debouncée) résout ce nom vers un vrai `PlaceSummary` Google via une recherche texte, avec repli sur l'ancien `PlaceSummary` synthétique (placeId vide) si la recherche échoue ou ne renvoie rien (`LogisticDetailsComponent.resolveAirportPlace`).
  - **Retour au jour d'origine après création Logistique depuis un jour** (décision : pas pour les Listes, pas de fin de cinématique définie côté liste) : `DayLogisticQuickAddService.create` transmet désormais le jour d'origine à `LogisticFocusService.requestFocus` (`originDayId`) ; `LogisticCardComponent.startGuidedEntry`/`LogisticDetailsComponent.startGuidedEntry` renvoient désormais une vraie `Promise` résolue à la fin du chaînage guidé, que `LogisticsListComponent.focusCardWhenReady` utilise pour déclencher `DayActivityFocusService.requestFocus(originDayId)` — mobile uniquement (`ViewportService.isMobile()`, la cinématique n'existe pas sur desktop).
  - **Nombre de fichiers dans le résumé** : nouveau `TripSummaryComponent.fileCount` (somme des fichiers de toutes les activités de pool + tous les éléments logistiques du trip), affiché dans la tuile "Résumé".
  - **Nouveau voyage, mobile** : comportement calqué sur la cinématique guidée des activités — à l'arrivée sur l'écran (`afterNextRender`), enchaînement automatique Ville (`TitleEditDialogComponent`, recherche Google ou texte libre) → Nom (`SimpleTextEntryDialogComponent`, texte simple) → Dates (`DatePickerComponent.openPanel`), auto-soumission dès la plage de dates complète. Les champs Ville/Nom deviennent des déclencheurs (boutons `.app-input-text`) rouvrant leur dialog respectif en dehors du chaînage initial.
  - **Nouveau voyage, desktop** : focus initial sur le champ Ville/Pays (`AutoCompleteComponent.focus()`, déjà exposé) via le même `afterNextRender`.
  - **Redirection à la connexion** — **à tester** (retour utilisateur du 2026-08-02 : logique à confirmer sur un vrai flux de connexion complet, plusieurs scénarios trip actif/inactif/0 trip) : nouveau type `TripSummary` (`trip.model.ts`, `earliestDay`/`latestDay` calculés côté `TripDataSource.getTrips$`/`TripStore.saveTrip` depuis les clés du map Firestore `days`, sans mapper complet) — `AccueilTripComponent` : 0 trip → `/trips/new` ; un trip **actif** (jour du jour dans l'intervalle `earliestDay`-`latestDay`) → directement ce trip ; sinon (aucun actif, un ou plusieurs) → reste sur l'accueil. Remplace l'ancien raccourci "1 seul trip → y aller" qui ne regardait pas les dates.
  - **Animation carte Résumé** : `SEGMENT_MS`/`RETURN_MS` réduits (4500/3600 → 3200/2500) sur demande utilisateur ("un peu" plus rapide) — `DWELL_MS` (pause sur chaque point) inchangé, seuls les déplacements sont concernés.
  - **Bouton "retour" navigateur** : `TripDetailComponent.updateFragment` utilise désormais `Location.go` (push, annulable par "retour") au lieu de `Location.replaceState` (qui écrasait systématiquement l'entrée courante, rendant le retour inopérant) — `Location.go` ne déclenche jamais `popstate` lui-même, donc pas de boucle avec le nouveau `bindPopState` (consomme les VRAIS retours/avances pour resynchroniser `activeDay`/le swiper sans repousser d'entrée). Effet de bord découvert et corrigé dans la foulée : `TripsComponent.currentUrl` (titre de la toolbar/`showBack`) ne réagissait qu'aux `NavigationEnd` du Router, jamais aux changements de fragment "bruts" (`Location.go`/retour navigateur) — restait donc figé sur la première URL sans fragment et retombait sur "NestTrip" après un retour ; `currentUrl` fusionne maintenant `router.events` ET `Location.subscribe`, et la regex d'extraction d'id (`toolbarTitle`) exclut désormais `#` (`[^/?#]+`) pour ne pas capturer le fragment dans l'id. Vérifié en conditions réelles (Playwright, chaîne complète tab→tab→retour×4→avance×2, titre et onglet actif corrects à chaque étape).
  - **Enlever "Autre" du menu "+"** : le menu à 7 entrées (uniquement sur un jour désormais, voir plus bas) filtre le type `other` des types logistiques proposés — reste un type valide côté données/formulaire, juste retiré de ce menu de création rapide.
  - **Libellés barre mobile agrandis** : `.tab`/`.day-chip` (0.75rem → 0.8125rem), `.subitem` (0.625rem → 0.6875rem) dans `MobileTripNavComponent` — au passage, `.subitem span` gagne un `text-overflow: ellipsis` (labels plus longs possibles depuis le renommage "Logement & Transport").
  - **Association Liste ↔ Activité** (décision : bidirectionnelle, stockage unique sur l'`Item` liste) : nouveau champ optionnel `Item.linkedActivityInstanceId` (`notes.model.ts`, pas de mapper séparé — ce modèle est déjà utilisé tel quel côté Firestore). `TripStore.getLinkedNoteItems`/`getDayActivityWithDay` (nouveaux sélecteurs) permettent la recherche inverse côté activité (aucune donnée dupliquée là) et la résolution jour+vue d'une instance potentiellement supprimée depuis (repli `undefined`, pas de nettoyage en cascade des liens). Deux nouveaux tiroirs de sélection, liste plate + en-têtes de section (même pattern que `LogisticsListComponent.typeRows`) : `LinkActivityDialogComponent` (depuis une Liste, activités groupées par jour chronologique, dates visibles) et `LinkListDialogComponent` (depuis une Activité, liste plate des Listes du trip). Chip cliquable des deux côtés (`app-chip`, removable) : côté Liste navigue vers le jour+activité (`DayActivityFocusService`), côté Activité vers l'onglet Listes (`NotesFocusService.requestFocus`, nouvelle capacité — auparavant `requestCreate` seul ; l'effect manquant dans `TripDetailComponent` pour basculer vers le tab Listes a été ajouté au passage, absent jusqu'ici).
  - **Logo du train** : PrimeIcons n'a aucune icône train/rail — nouvelle icône maison `.nt-icon-train` (SVG en `mask-image`, respecte `currentColor` comme un glyphe PrimeIcons, voir `src/styles/icons.scss`), décision actée avec l'utilisateur plutôt que de forcer une icône PrimeIcons sans rapport.
  - **Clic sur date/heure du header ouvre l'éditeur correspondant** : `ActivityHeaderComponent`/`LogisticHeaderComponent` émettent désormais des events dédiés (`timeClicked`/`dateTimeClicked`) au clic sur chaque fragment date OU heure (séparés pour la logistique : début/fin × date/heure, 4 zones) — `ActivityCardComponent`/`LogisticCardComponent` déplient la carte puis appellent le picker correspondant sur le form (`ActivityFormComponent.openStartTimeEditor`/`LogisticDetailsComponent.openStart{Date,Time}`/`openEnd{Date,Time}`), tiroir mobile ou champ déjà visible sur desktop.
  - **Blocage date/heure de fin < date/heure de début** : `DatePickerComponent` gagne un input `minDate` (jours antérieurs grisés/non cliquables dans le panneau ET saisie clavier desktop ignorée) ; `TimePickerDialogComponent` gagne un input `minTime` (toute sélection antérieure est remontée à cette valeur plutôt que rejetée silencieusement). Activités : `endTime` contraint par `startTime` (pas de champ date, une activité reste sur un seul jour). Logistique : `endDate` contraint par `startDate`, `endTime` par `startTime` UNIQUEMENT si début/fin tombent le même jour (sinon toute heure de fin est valide).
  - **Recherche dans Listes** : même pattern que `TripActivitiesComponent` (icône + champ + bouton croix) — matche titre OU texte de n'importe quel élément de la liste. Le drag-and-drop (réordonnancement manuel) est désactivé pendant une recherche active (`[cdkDragDisabled]` + garde dans `onDrop`) : `reorderItems` remplace la liste complète du trip, un drop sur une liste filtrée aurait fait perdre les items masqués.
  - **Style tuile "Tâches"** (retour utilisateur : un bord gauche seul, sans bord ailleurs, faisait "bizarre" ; des bords partout aurait été trop lourd) : remplacé par un point de couleur (`.trip-tasks-tile__dot`) + un léger lavis de fond dans la même teinte (`color-mix`, ~7% d'opacité, ~14% au survol) sur toute la carte, plutôt qu'une bordure.
  - **Bouton "+" contextuel dans le pool** (précise la décision du 2026-07-31, voir plan d'exécution en tête) : le menu à 7 entrées ne s'ouvre plus que depuis un jour (ou l'onglet Résumé, faute d'action contextuelle évidente) ; sur Activités/Logistique/Listes, le "+" redevient une création directe propre à l'écran (`TripCreationTargetService.register('logistics'/'notes', ...)`, réintroduit — déjà en place pour `'activities'`), icône/aria-label du bouton flottant contextuels (`TripDetailComponent.fabIcon`/`fabAriaLabel`, désormais des `computed`).
- **Session Bugs / fixes, 2026-08-01/02** : les 12 items de cette section, avec un aller-retour de retours utilisateur sur certains (détails ci-dessous).
  - **Drag-and-drop pool, survol raté quand le calendrier s'ouvre sous la boule** : un premier correctif (réévaluer le survol depuis `registerDayCells` avec la dernière position connue du pointeur) s'est révélé insuffisant — confirmé encore cassé par l'utilisateur. Cause racine réelle trouvée par repro Playwright instrumentée (geste pointerdown/hold/pointerup réel, sans mouvement) : pendant toute l'animation d'ouverture du sheet (croissance CSS de `height`, ~300-650ms), les rects des cellules jour sont une **cible mouvante** — `ActivityDayDispatchOverlayComponent.sheetExpanded` bascule à `true` dès le DÉBUT de cette transition (pour la déclencher), pas une fois "pleinement déployé" comme l'affirmait son commentaire ; `startEdgeAutoScroll` s'appuyait dessus pour armer sa zone de détection de bord, donc tournait (et re-capturait les rects, et parfois auto-scrollait) tout au long de l'animation, sur une géométrie qui n'avait pas fini de bouger. Nouveau signal dédié `sheetSettled` (bascule à `true` seulement au `transitionend` réel de la hauteur du sheet), utilisé par `startEdgeAutoScroll` à la place de `sheetExpanded`. `bindSheetTransitionEnd` capture aussi désormais les transitions individuelles (en cascade, `transitionDelay`) de chaque cellule jour, pas seulement celle du sheet. Reproduit puis vérifié corrigé en conditions réelles (Playwright, geste de bout en bout : l'activité apparaît bien sur le jour ciblé).
  - **Animation cdkDrag manquante (notes/todos)** : il manquait la règle CSS `.cdk-drop-list-dragging .cdk-drag:not(.cdk-drag-placeholder) { transition: transform ... }` (`src/styles/animations.scss`) — CDK réordonne bien les cartes survolées via un `transform` inline pendant le drag, mais sans transition dessus tant que cette règle n'est pas fournie par l'appli.
  - **Scroll auto sur l'activité déposée après un drag-and-drop** : deux cas distincts.
    - Inter-jours (calendrier) : `TripStore.dispatchActivity`/`attachPoolActivityToDay` retournent l'id de l'instance déposée ; `ActivityDayDispatchOverlayComponent` appelle `DayActivityFocusService.requestFocus(dayKey, instanceId)` juste après (même mécanisme que la navigation croisée déjà utilisée par la carte Résumé), ce qui bascule sur le bon jour ET scrolle vers l'activité une fois ce jour actif.
    - Intra-jour (réorganisation manuelle par poignée, cartes dépliées) : cas remonté séparément par l'utilisateur (rien ne scrollait après un drop, ex. carte 1 déplacée en position 4 dans une liste de 4) — `DayReorderService.handleDragPointerUp` ne faisait qu'un `wakeLoop()` (suivi caméra) après le règlement du drag, jamais de scroll réel. Ajout d'un appel à `DayScrollSyncService.focusActivity(movedId)` (2 `requestAnimationFrame` après `restoreCollapseSnapshot()`, pour laisser le re-dépli des cartes se peindre avant de mesurer la position finale), uniquement si l'ordre a réellement changé.
  - **Aiguilles du cadran horaire sans animation** : deux itérations.
    - Un premier correctif (transition CSS sur `transform`/`height`/`top` ensemble) donnait un mouvement de redimensionnement pendant la rotation plutôt qu'un vrai balayage — retour utilisateur : l'aiguille doit tourner jusqu'à la position sans jamais changer de taille, pivot toujours fixe au centre. Corrigé : seule `transform` est désormais animée (200ms) ; `height`/`top` (longueur du cadran, différente heures/minutes) changent instantanément, dans la même frame que le nouvel angle — l'aiguille tourne donc à longueur constante autour de son pivot fixe (`transform-origin: center bottom`).
    - Retour utilisateur suivant : la pastille bleue (`.clock-selector`, mise en avant de l'heure/minute sélectionnée) devait rester accrochée à la pointe de l'aiguille et balayer le même arc (passer par toutes les heures intermédiaires), pas rejoindre sa nouvelle position en ligne droite — elle était positionnée via `left`/`top` calculés en trigonométrie puis transitionnés indépendamment (interpolation en corde, pas en arc). Corrigé en la faisant porter par un `.clock-selector-arm` invisible, avec EXACTEMENT la même géométrie que `.clock-hand` (même `[ngStyle]="handStyle"` posé sur les deux, même transition) — la pastille, enfant positionné à la pointe de cet arm, est donc structurellement garantie de suivre l'aiguille à l'identique en toutes circonstances.
  - **Clignotement des photos d'activité (visionneuse plein écran)** : le filtre `placeholderSrc` de PhotoSwipe (`PhotoViewerService`) renvoyait toujours la miniature de la photo cliquée, pour TOUS les slides — alors que PhotoSwipe ne prévoit un placeholder image que pour le tout premier slide (`content.slide.isFirstSlide`). En swipant vers une autre photo, l'ancienne miniature s'affichait donc comme placeholder le temps que la vraie image charge. Non un bug de la librairie : un filtre applicatif qui outrepassait sa propre règle.
  - **Pull-to-refresh cassé sur l'écran swiper** — **à tester** (retour utilisateur explicite : laisser en l'état tant que non confirmé sur un vrai mobile, pas de nouvelle tentative pour l'instant) : cause réelle trouvée dans Swiper (`onTouchMove`, `swiper-core.mjs`) — avec `threshold` à 0 (défaut), Swiper appelle `preventDefault()` dès le premier pixel de mouvement, avant même d'avoir assez de données (~5px) pour déterminer si le geste est horizontal ou vertical ; un seul `preventDefault()` sur un touchmove désengage définitivement le geste natif de pull-to-refresh pour tout le reste du geste, même si Swiper se rend compte juste après que c'était un scroll vertical. `threshold: 10` (`TripDaySwiperComponent.setupSwiper`) fait sortir `onTouchMove` avant ce premier appel tant que la direction n'est pas connue — geste natif non simulable via Playwright, donc non vérifiable en conditions réelles depuis cet environnement.
  - **Bordure épaisse des panels de groupe (pool, vue ville/jour/à-assigner) invisible** : la règle CSS était syntaxiquement correcte (`:host(.app-panel--thick-border)`), le bug était une différence trop subtile (1px hairline vs 2px thick) pour se lire comme "plus épaisse" — vérifié par capture d'écran/mesure de style calculé (Playwright). Nouveau token dédié `--nt-border-width-group` (3px, `tokens.scss`), distinct de `--nt-border-width-thick` (2px, toujours utilisé par le chevron d'`app-panel`).
  - **Border-left noir sur les cartes "à assigner"** : `.not-dispatched > app-panel` réécrivait `border-left-style: inset` (effet biseauté assombri du navigateur) par-dessus le `border-style: dashed` posé sur les 3 autres côtés — retiré, le bord gauche reste `dashed` comme le reste, dans la couleur de statut normale.
  - **Modification du titre du trip qui rafraîchissait toute la page** : `TripStore.updateTripTitle` écrivait directement dans `_trips` (comme `updateTripCurrency` avant son propre correctif), ce qui faisait recalculer `activeTrip()` (nouvelle référence de `trip.days` à CHAQUE frappe) et donc tout ce qui en dépend dans l'app. Même traitement que la devise : signal dédié `_tripTitle` + sélecteur `getTripTitle(tripId)`, `updateTripTitle(tripId, title)` ne touche plus `_trips`. `TripHeaderComponent`/`TripsComponent.toolbarTitle` lisent désormais ce sélecteur (ou l'input `title` déjà fourni) au lieu de `trip()?.title`/`activeTrip()?.title`.
  - **Popups plein écran, clavier mobile ignoré** — **à tester** (retour utilisateur explicite, même raison que le pull-to-refresh) : nouveau `VisualViewportService` (root, instancié au bootstrap comme `ThemeService`) tient à jour `--nt-visual-viewport-height` (`window.visualViewport.height`, pas `100vh`/`100dvh` — vérifié que `dvh` ne suit pas l'ouverture du clavier virtuel de façon fiable) ; consommée par `.cdk-global-overlay-wrapper` (recentrage) et `max-height` des panneaux (`dialog.scss`/`field-edit-dialogs.scss`) — clavier virtuel non simulable via Playwright, donc non vérifiable en conditions réelles depuis cet environnement.
  - **Padding non uniforme du formulaire d'activité** : Début/Fin/Durée (`.custom-time-input-trigger`, `.time-fields--flat .time-box`), Prix mobile (`.price-field-trigger`, qui n'avait du padding qu'en haut/bas) et Deadline (`.app-date-picker__trigger`/`.app-date-picker__input`, oubliée dans un premier correctif — retour utilisateur) passent au même padding que Type/Résa/Prix desktop (`.compact-form .app-select__trigger`/`.app-input-text`, déjà uniformes). Valeur exprimée via le token existant `--nt-layout-gap-stacked` (`tokens.scss`, déjà la référence pour ce même 0.25rem ailleurs dans l'app, ex. `.gap-1`) plutôt qu'en dur comme un premier essai — retour utilisateur. Vérifié par mesure de style calculé (Playwright) sur les 7 champs (mobile et desktop), y compris la zone Deadline en contexte réel (statut "à réserver").
  - **Bouton "Ajouter" (pièce jointe) trop large** : passe en bouton icône seule (`size="small"`, sans `label`) dès qu'au moins un fichier est déjà déposé, pour se rapprocher de la largeur d'un `app-chip` au lieu de rester un bouton pleine largeur à côté des chips de fichiers.
- **UX / Interactions, lot du 2026-08-01** : cinq items non prioritaires restants de cette section.
  - **Icônes de carte** : `TripDayMapComponent.markerContent` affiche désormais une vignette (miniature de la 1re photo Google de l'activité, `GooglePhotoService`, cercle bordé coloré selon sélection) à la place du numéro, sur la carte du jour ET celle du pool (Résumé) — `DayMapPoint` porte un `photoRef?` optionnel peuplé par `DayPanelComponent`/`TripSummaryComponent` ; fallback (pas de photo/pas encore chargée/échec) sur le `PinElement` existant mais sans `glyphText` (plus de numéro nulle part).
  - **Tuile "Tâches"** (`TripTasksTileComponent`, nouveau, sous `trip-summary/trip-tasks-tile/`) : pleine largeur sous les tuiles Dépenses/Résumé, agrège activités à réserver/en liste d'attente (`DayActivityFocusService`) et réservations logistiques à réserver (`LogisticFocusService`), triées deadline d'abord (peu importe le statut, la plus proche en premier — décision actée le jour même, y compris pour une activité en liste d'attente avec deadline) puis logements à réserver / transports à réserver / activités à réserver / liste d'attente sans deadline ; masquée si vide.
  - **Fond de page** : nouveau token `--nt-page-background` (gris très clair en mode clair `--nt-surface-50`, quasi-noir dédié en dark `--nt-surface-950`, distinct de `--nt-content-background` des cartes) posé sur `body` (`reset.scss`) et repris par la toolbar/la carte épinglée en mode empilé (`trips.component.scss`/`day-panel.component.scss`), qui utilisaient jusqu'ici le mot-clé CSS système `canvas` pour imiter le fond de page.
  - **Calendrier au double-clic sur "Jour N"** (nav mobile uniquement — en desktop tous les jours sont déjà des tabs cliquables) : nouveau `DayPickerSheetComponent` (sous `mobile-trip-nav/day-picker-sheet/`), sheet montant depuis le bas de l'écran (0 → 50vh) qui reprend fidèlement l'animation/le markup du calendrier de `ActivityDayDispatchOverlayComponent` (drag-and-drop d'activité, demande explicite de l'utilisateur) mais sans sa mécanique de bulle/drag (propre à un vrai geste de drag, sans objet ici) — clic sur un jour navigue et referme avec la même transition (symétrique de l'ouverture), clic sur le fond ferme sans sélection.
  - **Clic sur une activité de la carte Résumé** : navigue désormais vers le bon jour/activité (`TripSummaryComponent` s'abonne à `map.activitySelected`, même pattern que `DayScrollSyncService`, et appelle `DayActivityFocusService.requestFocus`), en plus du recentrage de caméra déjà existant — `allPlacedActivities`/`generalMapPoints` gardent désormais l'association jour/activité (`DayMapPoint.dayId`, nouveau champ optionnel) nécessaire à cette navigation.
- **Nouvel onglet "Résumé", 2026-08-01** : 4e tab de premier niveau (`TripDetailComponent.tabs`, `GENERAL_TAB_IDS`), en tête avant Activités/Logistique/Listes, sur mobile ET desktop (`tabs()` étant déjà partagé entre `MobileTripNavComponent` et `TripTabsNavComponent`, l'onglet apparaît des deux côtés sans code dédié). Contenu (`TripSummaryComponent`, nouveau) : le header voyage (voir plus bas) + la carte du pool d'activités (reprise à l'identique, même mécanique `TripDayMapHostService`/`GeneralMapCinematicService` et cinématique de déplacement, déplacée depuis l'ancien onglet Activités, affichée seulement si des activités ont un lieu Google) + une tuile "Dépenses" (devise + somme des prix de toutes les `DayActivityInstance` du trip, un placement sur 2 jours comptant 2 fois, toutes devises additionnées sans conversion — sujet multi-devise resté ouvert par ailleurs, voir "Devise") + une tuile "Résumé" à côté, même hauteur (nombre d'activités = nombre de placements, nombre de transport = réservations Vol/Train/Location voiture uniquement, nombre de jours).
  - **Header voyage devenu contenu normal de Résumé, plus de chrome fixe (correctif same-day)** : `TripHeaderComponent`/`TripCollaboratorsComponent` ne vivent plus dans `TripDetailComponent` (fixed, caché/affiché selon l'onglet) mais directement dans le template de `TripSummaryComponent`, en flux normal — scrolle avec le reste du contenu de cet onglet au lieu d'être épinglé sous la toolbar. `onTitleChange`/`onDatesChange`/`buildDays`/`findDaysToAdd`/`findDaysToDelete` déménagés avec lui (simplifiés : plus de bascule forcée vers "Activités" après un changement de dates, on reste sur Résumé). `TripChromeService` perd toute notion de "header" (`ChromeKey` réduit à `'toolbar' | 'tabsNav'`, `headerHeight`/`headerTop`/`headerStackHeight` supprimés) — corrige un gap-1 (0.25rem) fantôme qui restait entre la toolbar et le contenu sur tous les onglets (la formule `contentTopOffset` réservait un espace pour un header dont la hauteur mesurée valait déjà 0, au lieu de ne rien réserver du tout).
  - **Carte sortie de son `app-panel` repliable dans ce contexte** : `TripDayMapComponent` (instance UNIQUE partagée jour/pool) est toujours enveloppée par un `app-panel`, mais celui-ci expose maintenant un mode `bare` (nouvel input sur `PanelComponent`, `false` par défaut partout ailleurs) qui masque entièrement son header/chevron/bordure — activé uniquement quand `TripDayMapHostService.currentOwner() === 'general'` (donc uniquement dans Résumé désormais). La carte n'est alors plus jamais repliable dans ce contexte (`collapsed` vaut `false` en dur pour 'general', `GeneralMapPanelService` — qui ne servait plus qu'à ça — supprimé). Correctif nécessaire dans la foulée : `PanelComponent.fillHeight`/le `height:'100%'` du `google-map` interne ne s'activent plus non plus pour 'general' (`TripDayMapComponent.useSplitHeight`), sans quoi la carte héritait du comportement "layout scindé" de l'ancien onglet Activités (hauteur 100% contre un parent sans hauteur définie) et se retrouvait invisible (0px de haut) en largeur desktop.
  - Toolbar : nom du voyage à la place de "NestTrip" sur l'écran de détail (`TripsComponent.toolbarTitle`, basé sur le même test de route que `showBack`), tronqué en CSS (ellipsis + tooltip natif) si trop long.
  - Carte retirée de l'onglet Activités (`TripActivitiesComponent`) avec tout ce qui n'avait de sens qu'avec elle (`DayScrollSyncService`, `GeneralMapCinematicService`, `TripDayMapHostService`).
- paramétrer la récup des infos du trafic d'avion (non prioritaire)
- **Refonte navigation mobile bas d'écran, 2026-08-01** : les deux barres empilées en bas sur mobile (`TripTabsNavComponent` + `GeneralSubTabBarComponent`, cette dernière supprimée) sont remplacées par une seule barre morphing (`MobileTripNavComponent`) entre un état "Jours" (bande de jours scrollable au-dessus + badge "Jour N"/"Général") et un état "Général" (bande masquée, icône compacte "Jour N", 3 sous-items Activités/Logistique/Listes en cascade avec pastille glissante) — s'applique aussi en mobile paysage tactile, pas seulement portrait (`ViewportService.isMobileChrome`, nouveau critère `pointer: coarse` pour distinguer un mobile tourné d'un vrai desktop en fenêtre étroite, seul cas qui garde l'ancien layout scindé). Dans la foulée, Activités/Logistique/Listes (renommé depuis "Notes" — seul le libellé, pas l'id technique `'notes'` ni les champs de texte libre "Notes" des activités/logistiques) deviennent 3 tabs/slides de premier niveau du swiper PARTOUT (mobile et desktop) au lieu d'un unique onglet "Général" avec switch interne (`GeneralPanelComponent`, dissous) — un swipe entre eux puis vers Jour 1 met à jour la barre/le tab du haut sans câblage supplémentaire (état dérivé de `activeId()`). Sur mobile, le calendrier de dépose d'activité (drag-and-drop) apparaît désormais directement, sans réplique/FLIP de l'ancienne barre (le mécanisme desktop, lui, continue de fonctionner à l'identique, généralisé pour 3 tabs non-jour au lieu d'1).
- **Lot de correctifs bugs, 2026-07-31** :
  - Sélection d'un vol via l'API (chaînage guidé mobile Logistique) : les infos récupérées (compagnie, aéroports, horaires) n'apparaissaient pas dans le formulaire — `LogisticDetailsComponent.guidedFlight()` écrivait le résultat du lookup directement dans le store (`tripFacade.updateLogistic`) mais jamais dans l'état local (`form`/`selectedPlaces`) que le template lit réellement, et l'effect qui resynchronise habituellement l'un depuis l'autre est volontairement suppressé pendant `guidedFlowActive`. Ajout d'un miroir local (`form.patchValue(..., {emitEvent:false})` + `selectedPlaces.update(...)`) juste après l'écriture store, qui évite aussi qu'un commit débounce ultérieur (déclenché par l'étape Résa suivante, sur le même form) n'écrase la donnée fraîchement récupérée avec les anciennes valeurs locales vides.
  - Vue "Lieux" : la couleur de statut de réservation (bord gauche) et le pictogramme trombone n'ont de sens que rapportés à un jour précis, pas dans une vue organisée par lieu — `ActivityCardComponent.hideBookingMeta`, posé sur TOUTES les cartes de cette vue (fusionnées ou non, retour utilisateur du 2026-07-31 : masquer seulement sur les cartes fusionnées créait une incohérence, certaines cartes gardant la couleur et pas d'autres), masque désormais les deux partout dans cette vue — la classe `.booking` (bord gauche 0.4rem) n'est simplement plus posée du tout dans ce cas, la carte retombe sur le contour hairline par défaut de `app-panel`. Un premier essai avait gardé la largeur 0.4rem avec une couleur neutralisée : régression relevée par l'utilisateur (2026-07-31) — ce bord gauche épais, même gris, entrait en concurrence visuelle avec le contour légèrement plus épais du panel de groupe "ville" (`thickBorder`) censé rester la seule bordure "plus large" de la hiérarchie ; retiré entièrement, pas seulement neutralisé.
  - Création d'une activité en double (bouton "+" du pool général, mobile) : contrairement à `DayActivityCreationService`/`LogisticsCreationService`, `TripActivitiesCreationService` n'avait aucun garde-fou contre un double-tap ouvrant deux `TitleEditDialogComponent` en parallèle — ajout du même principe (`mobileDialogOpen`), le temps que le tiroir soit ouvert.
  - Carte repliée (vue jour desktop, layout scindé) qui gardait `height: 100%` même repliée, laissant un grand vide sous son header : `PanelComponent` expose maintenant une classe hôte `app-panel--collapsed`, et la règle `fillHeight` (`panel.component.scss`) l'exclut désormais.
  - Activité créée depuis le pool puis aussitôt glissée sur un jour : créée en double sur ce jour (retour utilisateur du 2026-07-31, confirmé deux fois). Un premier correctif sur `TripFacade.mergeFromRemote` (garde anti-flicker manquante sur `_dayActivityIds`, gardée car réelle mais insuffisante) n'a pas suffi — cause racine trouvée par instrumentation (repro Playwright réelle, geste pointerdown/move/up simulé) : l'`effect()` de `ActivityDayDispatchOverlayComponent` qui exécute la commande réelle (`tripFacade.dispatchActivity`) à la lecture de `ActivityDispatchService.dropRequested()` se réexécutait DEUX FOIS pour EXACTEMENT le même objet requête, alors que `handlePointerUp` n'appelle `dropRequested.set(...)` qu'une seule fois par dépose — `dropRequested` ne repasse à `null` (fin du cycle) que bien après, à la toute fin de l'animation de la bulle (`finish()`, ~250 à 900ms plus tard), laissant une fenêtre où un second passage de l'effect (redéclenché par n'importe quel autre changement de signal traité dans le même cycle Angular) relit la même requête encore en place et redéclenche `dispatchActivity` une deuxième fois. Corrigé par une garde d'idempotence sur l'identité de l'objet requête (`lastDispatchedReq`) dans cet effect : `dispatchActivity` ne peut plus être appelé deux fois pour la même requête, quel que soit le nombre de fois où l'effect se réexécute entre-temps. Reproduit puis vérifié corrigé en conditions réelles (Playwright, geste de drag simulé de bout en bout).
  - Vérifiés sans correctif nécessaire : `ChangeDetectionStrategy.OnPush` est bien posé sur les 74 composants du projet (aucune régression) ; le multi-clic sur le "+" de l'onglet Logistique est déjà protégé par `LogisticsCreationService.creatingId` ; le padding case à cocher/poignée de drag évoqué pour les logistiques mobile ne s'applique plus (poignée de drag retirée avec le tri 100% automatique des réservations, voir plus haut).
- Vue vidéo avec animation qui parcourt le voyage (non prioritaire)
- Bouton "œil" pour visu avec animation vue macro (non prioritaire)
- Suppression multi-sélection unifiée (activités, réservations, notes, accueil-trip) : long-press + drawer sur mobile, checkbox toujours visible sur PC, `SelectionModeService` transverse + `TripItemDeletionService`
- Skeleton loading
- Refacto du style vers PrimeNG (composants + usage cohérent)
- Multiupload de fichiers
- Suppression de l'espacement clé/valeur dans les grids
- Déplacement automatique sur le bon jour
- Champs activité : horaires d'ouverture, carte (adresse), trajet, heure début/fin, type, prix, couleur d'état ("à réserver", "réservé", "pas besoin de réserver")
- Correction lien fichier + lien adresse maps
- Suppression du padding latéral des panel content
- Timeline cliquable vers Activités, sous forme de grid
- Un seul slot d'activités (fusion des slots multiples)
- Modification on input avec debounce 5s pour la mise à jour
- Format de date dans le storage header
- Navigation par jour selon date du jour / dernier jour ouvert (cookie)
- Note en début de page
- Nouveau style carte "activité" (2 lignes, photo Google Photos)
- Drag and drop des activités
- Popup de suppression réservée (accès restreint)
- Header modifiable et stylé (infos voyage)
- Menu de sélection/création de voyages
- Swipe gauche/droite entre les jours
- IA pour pré-remplir les infos à la saisie du titre
- Carte de couleur différente si réservation nécessaire
- Style responsive : login, popup, activités
- Toggle activité par défaut, nettoyage affichage (min, à réserver, heure)
- Calendrier pour modifier les dates début/fin du voyage (avec alerte si suppression de jours + proposition de décalage)
- Carte avec points d'activités du jour, cliquable vers l'activité dépliée
- Suppression d'un trip / d'une activité
- Réduction définition photos sur mobile
- Titre d'activité modifiable
- Déplacement d'activités entre jours via calendrier stylé (drag and drop qui se réduit + calendrier si pas de mouvement)
- Réservations transverses (hôtel/vol/location/autre) : sous-menu dédié (CRUD complet), bannière contextuelle read-only dans les jours (check-in/check-out, départ/arrivée, prise en charge/restitution), statut de vol temps réel via AeroDataBox avec rafraîchissement automatique intelligent (voir Reservation.md — nécessite de provisionner le secret `AERODATABOX_API_KEY` avant déploiement, pas encore testé sur de vrais vols)
- Autocomplete ville + nom de voyage ("Voyage à…")
- Suppression du mode lecture
- Clavier masqué quand le sélecteur est ouvert
- Swiper.js pour le slide
- Heure début/fin sur activités
- ID de day dans l'URL
- Fix slide mobile
- Vue "choix de la date" uniquement dans trip détail
- Animation toggle ralentie au drag, pop à la sélection, clavier qui ne s'ouvre plus
- Fix retour à l'écran de sélection après suppression de dates
- Fix lazy loading au retour sur un élément
- Fix couleur blanche résiduelle sur activeday au scroll
- Split ActivityCard (technique)
- Split infos (technique)
- Carrousel réduit par défaut après usage répété, form resserré, boutons/panels réduits
- Synchro carte/scroll au déplacement sur la carte
- Clic sur activité centre la carte
- Clic extérieur ferme tout ; heure début/fin harmonisées
- Logos remplacés par pi-chevron
- Bouton "tout supprimer" en footer
- Drag and drop désactive le slide
- Planificateur : liste d'activités à dispatcher sur les jours, triée
- Onglet "Général" : activités générales groupées par ville (ou "pas de lieu")
- Onglet "Notes" avec logo en top
- Onglet "Administratif" (hôtels, etc.) via boutons
- Overshoot avec vélocité au retour du drag and drop
- Onglets déplacés en bas (UI)
- Rappel couleur sur réservation
- Composant carte unique avec points injectés (prix des accès)
- Mode vecteur activé sur Google Maps
- Optimisation Maps : composant unique
- Optimisation appels Places (lazy avis, uniquement ceux manquants)
- Vue initiale centrée sur les 3 points en arrivant sur la carte
- Clavier masqué sur les datepickers
- Instance unique de la carte (post-portal)
- Animation swipe droite/retour au clic sur un élément de liste
- Date picker readonly dédié mobile
- Fix centrage carte sur scroll (au lieu du bouton 1)
- Time picker custom façon Google
- Suppression de trip limitée aux créateurs
- Fix rafraîchissement liste collaborateurs après invitation
- Fix drag and drop des notes
- Filtres "assigné / non assigné" en plus de "par ville"
- Changement du picto note (activité globale)
- Fix scroll carte
- Bouton flottant d'ajout
- "Google" au lieu de "GOOGLE"
- "Ajouter un voyage" (libellé)
- Collapse des activités sur l'écran pool
- p-panel → p-card pour notes et activités
- Chiffrement des fichiers stockés
- Réduction vitesse de scroll
- Variables d'environnement injectées et sorties du repo
- Fermeture de tous les composants ouverts au scroll du swipe
- Adaptation taille des jours (prend tout si inférieur)
- Hôtels/vols dans Général + liste complète
- Fix drag and drop activité cassé
- Complétion automatique de l'heure à la sélection
- Fix décalage "NestTrip" à cause de la flèche
- Fix rechargement de page lors de la modif d'une activity card
- Activités réelles au niveau des days (form indépendant par jour) + version "light" dans le pool, fichiers centralisés sur l'activité de pool : une même activité peut être placée sur plusieurs jours
- Login en `dvh` (prend en compte la barre Google)
- Lorsque je drag and drop les activités, certaine se mettent avec des heure de début et de fin non renseigné (le bon cas ), d'autre s'alimente avec l'heure actuelle, ce n'est pas bien !
- Vue d'ensemble avec zoom adapté pour voirs tous les point d'un jour quand on arrive sur un jour. Ensuite, lorsqeu l'on scroll juqu'à l'activité 1, il fautr que le zoom se positionne sur le jour 1, exactement comme la carte est aujourduit. Une fois arrivé à la, le zoom déjà en place par rapport au scrolling sera en place et tout fonctionnera.
- Je veux améliorer le zoom au scroll : il faudrait accélerer entre 2 point ert ralentir quand on est proche d'un point, là la trajectoir est linéaire
- Liste générale : rajouter l'info du/des jour(s) si l'activité est assignée (elle peut maintenant l'être sur plusieurs jours à la fois), mettre la mention "À assigner" sinon avec un truc visuel, une couleur.
- Hauteur de la carte Google en % d'écran plutôt qu'en pixels
- Taille minimum dynamique sur les fenêtres swipe
- Slide : repenser le slide: metttre le slide directement dans chaque slider, comme ça, en allant au jour suivant le slide d'un élément n'est pas pollué par le slide précédant. Par contre, il faut une facon élégante de masquer le header fixe + le header de voyage en scollant vers le bas. On pourrait faire du ménage sur le calcul du window.scrollY sur le body, et il faut faire très attention au slide qui est calculé sur chaque slider pour caler les activités à la carte. Attention aussi à ne pas afficher le slider dans le slide je ne veux pas plusieurs assenseurs. Cela simplifiera peut etre aussi le sticky sur la carte qui était compliqué à réaliser.
- Le changement de type sur une activité ne fonctionne plus !
- Barre du bas incohérente ("Général") lors du drag and drop d'activités : la bar est recréé dasn el composant de drag and drop mais elle est uniquement bien simulé dasn l'onglet Général, on ne pourrait pas faire un clone du visuelle comme pour le drag and drop que tu as mis ? Comme ça si il y a des changements futur, pas besoin de faire de correctif
- Le drag and drop fonctionne pas sur mobile, il s'annule dès que je drag :le redémarrage de serveur avait pourtant corrigé ce beug en serveur local
- Navbar : chiffre du jour en gros, mois complet en plus petit en dessous. Il serait bien d'avoir le jour de la semaine aussi; et de l'élargir un peut car sur les portables, si il y a la bar de multitache qui passe par dessus ça fait fin. Enfin, adapter la taille des jour si il y en as pas beaucoup pour qu'il prennent toute la place. peut être faire un custom plutôt ? avec le thème primeux attention
- Fix : sélection décalée vers le haut si un panel se réduit pendant le drag custom
- Si on ajoute des activités sur le pool, l'ordre ne doit pas changer.
- au niveau du slide de la carte, il faudrait calculer la hauteur à prendre en fonction de la distance à parcourir : plus il y a de distance, plus il faut reculer. Moins il y en a, moins il faut reculer
- Pouvoir supprimer des collaborateurs (via un clique sur les pastilles affiché:  ouvre la popup d'ajout des collob modifié qui permet la gestion des collab associés à ce trip, l'ajout d'un nouveau collab, la selection d'un companion de route à ajouter en clique rapide depuis une liste, la suppression d'un collab de route via une croix. Il faut donc stocker la liste des companions de route sur l'utilisateur, ou ? nouvelle bdd user ? => dans firestore ? Ca pourra servire pour le stockage des reglages du user. faut faire attention à la gestion des user, seul l'owner peut supprimer des membres, et il ne peut pas se supprimer lui même et doit supprimer le voyage, information à donner si son nom est grisé. Parti du dialog existant au clique sur "add collaborator". On pourrait supprimer le bouton "add colaborator" car le cluque sur la pastille remplacerai ça ?
- Et sortir le p-dialog pour le mettre dasn un composant à part, transverse comme ça sa taille n'est pas limité au composant
- Ouvrir le calendrier en dialog si écran taille smartphone
- Tooltip propriétaire à repositionner sur la suppression des activité car il est parfois mal positionné et un top, bottom ne regle pas 100% des cas
- Zones de drag and drop plus larges que les logos (réduire les mis-clics)
- p-drawer mobile-only pour le mode modification de listes (desktop garde le fonctionnement actuel)
- Crayon pour modifier le titre pour pouvoir mettre le clique sur tout le header pour le déplier
- Lorsque je choisi une activité dans le pool via la bar, les infos googles ne sont pas rensiegnés. Au deuxième clique, là ça marche. A corriger pour que ça marche du premier coup
- La zone de saisie pour le prix n'est pas bonne
- L'ajout des activité dans le pool ne chaine pas sur la saisie du titre
- Si j'ajoute un titre à une activité dans le p-autocomplete, la valeur du titre est bien mit à jour mais les données de google ne sont pas ramené, alors que la deuxième fois si
- Bouton flottant d'ajout avec scroll auto vers la nouvelle activité créée et le curseur positionné sur le choix de l'activité
- Uniformiser les pratique entre flouter et mettre en plus sombre quand il y a un modal
- Afficher la carte seulement si pas d'activité ou activités incomplètes
- Problème theme : skeleton bleu en dark mode, chip des pieces en thème dark sont en couleur claire, le contour de app overlay n'est pas la bonne couleur ni en dark ni en light
- Image : suppression du bloc image, l'ouverture des images doit se faire via le clique sur l'image miniature → embla-carousel en p-dialog sur toute la page
- Onglet Activités : tri par ville (avec fusion des doublons de même placeId, dates combinées) ou chronologique (jour par jour, "à assigner" en haut, jamais fusionné), barre de recherche, clic sur une date pour sauter au jour + scroller vers l'activité
- Durée : autre méthode de saisie, gestion des durées > 24h si on a plusieurs jour plusieurs jours
- Inciter l'utilisateur à compléter les données d'une carte/activité transport/hébergement en ouvrant l'élément à modifier suivant à la création d'une activité ? Ca ferai comme un pipe ou il chaine sur les 2-3 infos, pas trop relou en terme d'ux ?
- Modifier le système de saisie des horaire sur ordi pour faire 2 zones de saisie number : pas d'ouverture du composant
- Modifier le composant timepicker pour permettre la saisie sur clavier comme sur le composant google !
- Dans la modification de l'heure, si l'utilisateur positionne une heure puis une minute, alors il faut faire ok
- Si j'ai qu'un seul trip, je ne peux plus faire retour sur la première page, donc je ne peux pas créer de trip. C'est à la connexion qu'il faut aller sur le trip, pas tout le temps
- style zones saisie sur ordi + taille durée qui n'est pas bon quand on est pas en mode horloge (trop haut) + durée tu as fais du spécifique ? Il faudrait pas et réutiliser le même composant. min-height: 16.5rem; à enlever quand pas horloge
- Saisir duré : faire le focus sur l'heure
- Warnings de dépréciation Google Maps (`PinElement.element`, `click` → `gmp-click`)
- Couleur de la barre de drag&drop maison : sélecteur CSS `.p-panel` obsolète (mort depuis la sortie de PrimeNG) remplacé par `.booking`
- Bouton flottant trop près du bord (marge + `safe-area-inset-right`) + animation d'ouverture ralentie
- `min-width: 24rem` remonté au niveau `html`/`body` : protège aussi les éléments passés en `position: fixed` (le swiper), que le `min-width` local de `.app-content` ne couvrait plus
- Statut ouvert/fermé Google basé sur l'heure de début de l'activité, plus l'heure de consultation
- Thème clair/sombre/système : `ThemeService`, bouton segmenté 3 icônes (même style qu'Activités/Réservations/Notes) dans le menu réglages, avec le libellé "Thème" ; carte Google Maps recréée à chaque bascule pour un rendu correct (`colorScheme` ne se rafraîchit pas visuellement via `setOptions()` sur une carte déjà créée) ; `color-scheme` posé explicitement sur `:root[data-theme]` pour que `light-dark()` (classes utilitaires) suive le thème choisi
- Max-width centré sur les colonnes jour/général en grand écran (`--nt-content-max-width`)
- Logo pièce jointe dans le header d'activité si des fichiers sont associés
- Largeur max du tab de login sur PC
- Indicateur discret de sauvegarde : fine barre qui glisse de gauche à droite (verte si succès, rouge en boucle si échec), sans texte
- État de l'onglet Général (`?tab=`) et du tri Activités (`?sort=`) dans l'URL, restaurés au montage
- Catégorisation en cours/future/passée des réservations : passées grisées avec contour en pointillés (comme les activités non placées), tri désormais 100% automatique (glisser-déposer manuel retiré)
- Devise par défaut par voyage : sélecteur dans le header, préremplit les nouvelles activités/réservations (jamais rétroactif sur l'existant)
- Fix : le tri chronologique de l'onglet Activités affichait le formulaire d'édition à tort
- Fix : tenir appuyé sur la poignée de drag and drop (ou démarrer un drag) déclenchait le mode sélection à tort
- Fix : icônes du sélecteur (ex. thème) mal centrées quand un bouton n'a pas de label
- Clic n'importe où dans le header (pas seulement le bouton bascule) pour replier/déplier une carte (activités, notes, réservations, panneaux jour...), sauf sur une zone interactive (poignée de drag, image, bouton crayon, checkbox) — générique au niveau de `PanelComponent`, accessible au clavier (tabindex + Entrée/Espace)
- Redirection directe vers l'unique trip à l'ouverture de la web app (login interactif ET session déjà authentifiée restaurée au démarrage), jamais lors d'un retour manuel depuis un trip — flag `AuthService.justLoggedIn` initialisé à `true` à la construction du service (une seule fois par chargement de page), consommé une fois par `AccueilTripComponent`
- Tiroir mobile de `SelectComponent` : titre de la liste au lieu d'un input de recherche (voir régression du 2026-07-29 corrigée plus bas — les listes concernées sont toutes courtes, la recherche n'apportait rien)
- Préremplir le titre de la nouvelle activité avec le texte déjà tapé dans la barre de recherche
- Barre Activités/Réservations/Notes flottante en bas d'écran, au-dessus de la barre des jours, uniquement sur mobile (remplace la bascule inline en haut, cachée en dessous de 768px) — reste visible pendant le scroll sans avoir à remonter en haut de la liste
- Max-width sur les textarea de titre inline (titre du voyage, titres/points de notes) : sur grand écran ils s'étiraient sur toute la largeur de la colonne, rendant l'espace vide à droite du texte cliquable/focusable à tort (`.nt-capped-inline-field`)
- Carte épinglée (sticky) au-dessus de la liste d'activités du jour : ajout d'une ombre portée pour séparer visuellement la carte du contenu qui défile dessous, qui donnait l'impression d'un bug d'empilement
- Saisie clavier des dates dans `app-date-picker`, desktop uniquement : le trigger devient un vrai champ texte (`dd/MM/yyyy`, ou `dd/MM/yyyy - dd/MM/yyyy` en mode plage) à côté d'un bouton icône séparé pour ouvrir le calendrier — mobile inchangé (bouton seul)
- Infrastructure de travail autonome : 4 skills projet (`nesttrip-roadmap`, `nesttrip-verify`, `nesttrip-testing`, `nesttrip-e2e`), section "Workflow" dans `CLAUDE.md`, premiers tests unitaires réels (Vitest — mappers + `TripStore` avec ses services de persistence stubbés, anti-flicker `_pendingActivityIds` couvert), et socle e2e Playwright (compte de test dédié, parcours login + création de trip couverts)
- Tous les composants passés en `ChangeDetectionStrategy.OnPush` : 3 `ControlValueAccessor` corrigés au passage (InputNumber, Password, TimePickerDialog — état en signal plutôt qu'en champ simple, `writeValue()`/`setDisabledState()` étant appelés depuis l'extérieur du template) ; `provideZonelessChangeDetection()` ajouté explicitement (l'app tournait déjà sans `zone.js`, en fallback implicite) et 3 usages `NgZone` devenus des no-ops nettoyés
- SCSS custom dupliquant une classe PrimeFlex existante basculé vers ces classes utilitaires (triage fichier par fichier, uniquement quand la valeur est strictement identique et sans redéfinition locale en conflit) puis suppression complète de la dépendance PrimeFlex : sous-ensemble réellement utilisé dupliqué dans `src/styles/layout-utilities.scss` (portage 1:1, mêmes noms/valeurs), shim `--p-*` de `tokens.scss` supprimé
- Montée Angular 22 (+ `@angular/cdk`, `@angular/google-maps`, `angular-eslint`, TypeScript 6) : `ng update` en un seul passage, migration automatique (`withXhr()` sur `provideHttpClient`), dernier `@ViewChild` décorateur converti en `viewChild()` signal
- Secret de déploiement pour la release — **en pause** : nécessite une valeur/action manuelle de l'utilisateur (créer le secret côté hébergeur/CI), pas actionnable par le développement seul.
- **Régressions post-migration OnPush/PrimeFlex/Angular 22 (remontées le 2026-07-29)** :
  - Cause racine du "drag and drop qui plante" (desktop) : `activityToFb` (mapper d'une activité de pool) spreadait l'objet domaine tel quel, laissant passer `address`/`latitude`/`longitude` à `undefined` pour une activité sans lieu Google (ex. saisie en texte libre) — Firestore rejette toute écriture contenant un champ `undefined` (`updateDoc`/`setDoc` lèvent une exception non rattrapée), retentée indéfiniment toutes les 3s par `DebounceWriter` (voir `debounced-writer.ts`). Corrigé à l'endroit prévu pour ça : `activityToFb`/`tripToFb` omettent maintenant les champs optionnels absents plutôt que de les écrire à `undefined`, même règle déjà appliquée par `reservationToFb` (voir sa doc) — `ignoreUndefinedProperties: true` gardé sur l'instance Firestore (`FirebaseService`) en filet de sécurité, mais le mapper reste la source de vérité pour la propreté des données
  - **Gel total au clic sur un jour en mode mobile** (confirmé par l'utilisateur comme antérieur à la migration, pas la même cause que le point Firestore ci-dessus) : cause tracée jusqu'à l'`effect()` de `ActivityCardComponent` qui repliait la carte Google pendant l'édition d'une activité sur mobile (`GoogleMapPanelService.beginEditLock/endEditLock`) — il rappelait ces méthodes à CHAQUE réévaluation de ses dépendances, pas seulement quand l'état verrouillé changeait vraiment, provoquant en cascade des allers-retours coûteux (déplacement DOM + resize Google Maps) dès qu'un jour affichait au moins une activité sur mobile. Un premier correctif (n'appeler le service que sur un vrai changement d'état) avait réglé le gel, mais la fonctionnalité "carte repliée pendant l'édition" elle-même a ensuite été retirée entièrement (voir plus bas, décision produit) — le gel a donc disparu avec elle. Garde-fous anti-emballement ajoutés en complément (arrêt + diagnostic console si une boucle `requestAnimationFrame` tourne anormalement longtemps sans jamais atteindre l'idle) dans `DayScrollSyncService`/`TripDaySwiperComponent`, qui n'étaient pas la cause ici mais restent une protection utile
  - **Carte repliée automatiquement pendant l'édition d'une activité (mobile)** : fonctionnalité entièrement retirée (`GoogleMapPanelService.beginEditLock/endEditLock` + l'effect associé dans `ActivityCardComponent`) — décision produit : cette piste de la roadmap a été écartée au profit de composants drawer dédiés (notes, devise) pour éditer sans se disputer l'espace avec la carte ; `isCollapsed`/`setCollapse`/`toggleCollapse` restent (toggle manuel + collapse pendant un drag and drop, usages indépendants)
  - Vue "chronologie" du pool d'activités : le message "Aucune activité pour l'instant" ne s'affiche plus en bas de liste (redondant avec la catégorisation par jour, déjà visible même vide)
  - Devise par défaut du trip : signal dédié (`TripStore._tripCurrency` / `TripFacade.getTripCurrency`), la modifier ne met plus à jour `_trips` (qui faisait recalculer `activeTrip`, lu par énormément de composants, et rafraîchissait tout l'écran à chaque changement)
  - Heure de fin/minutes d'une activité (et check-in/check-out d'une réservation, même composant `app-time-picker-dialog`) renseignées à tort avec l'heure/minute actuelles lors d'une saisie clavier partielle (desktop) : le champ retombait sur `new Date()` (l'instant de la frappe) au lieu d'une base neutre 00:00 quand aucune heure n'était encore saisie
  - "Fermé" affiché à tort sur un établissement ouvert 24h/24 (`ActivityGoogleInfoComponent.isOpenNow` ne reconnaissait pas la mention "24h/24", faute de plage horaire `hh:mm–hh:mm` à parser) ; bloc "Horaires d'ouverture" masqué quand la liste est vide plutôt qu'affiché sans contenu
  - Texte barré des notes (`.line-through`) : classe utilitaire oubliée lors du portage du sous-ensemble PrimeFlex vers `layout-utilities.scss`
  - Zone de saisie des dates du jour/voyage (`app-date-picker`, desktop) tronquée depuis le passage de l'année à 4 chiffres : largeur mini en `ch` dimensionnée pour le format le plus long (`dd/MM/yyyy` ou plage complète), `flex:1` (flex-basis 0) ignorait sinon la taille du contenu dans un host `width:max-content`
  - Bouton flottant "Ajouter" qui passait par-dessus le bouton "Notes" de la barre de bascule mobile (Général) : sa hauteur (`TripChromeService.generalSubTabBarHeight()`) n'était pas prise en compte dans le décalage bas du bouton, seule la barre des jours l'était
  - Barre du mode sélection ("Annuler"/"Supprimer") plus petite que la barre des jours qu'elle doit recouvrir entièrement : hauteur mini alignée sur `TripChromeService.tabsNavHeight()`
  - Carte du jour : le clic sur un marqueur ne recentrait/scrollait plus vers l'activité correspondante — `gmpClickable` non activé sur les markers avancés dont l'écouteur `gmp-click` est posé à la main (`addEventListener`, pas le helper `addListener` historique qui l'activait implicitement) : le marker restait visuellement affiché mais insensible aux clics réels
- **Lot mobile du 2026-07-29 (suite)** :
  - Focus manquant sur le champ de saisie du tiroir plein écran `TitleEditDialogComponent` (titre d'activité, mobile) : l'autofocus CDK par défaut (`'first-tabbable'`) ciblait le bouton de fermeture (premier élément focusable du template), pas le champ — `autoFocus: '.title-edit-dialog__input'` posé explicitement sur les deux points d'ouverture (`ActivityHeaderComponent.openTitleEditDialog`, `DayActivityCreationService.startMobileCreation`), même pattern déjà utilisé par `TimePickerDialogComponent` (mode durée)
  - Réservations, dates jamais préremplies : `ReservationsCreationService.createReservation` fixait `startDateTime`/`endDateTime` à `new Date()` à la création — `Reservation.startDateTime`/`endDateTime` (domaine + Firebase) sont maintenant optionnels (même principe que `DayActivityInstance.startTime`/`endTime`), la réservation se crée sans date (`--:--`, champ vide) jusqu'à saisie ; consommateurs sécurisés : `getReservationDayOccurrences` (pas de bandeau tant que non daté), `allReservationsSorted`/`reservationsForDay` (une réservation sans date passe en tête de liste plutôt que d'être mélangée ou perdue), `ReservationCardComponent.timeStatus` (4ᵉ état `'undated'`, même contour pointillé que les activités non placées mais sans l'opacité réduite de `'past'`), `FlightStatusRefreshService` (pas de polling tant que la date de vol est absente)
  - Réservations : chaînage de saisie guidée mobile ajouté (`ReservationDetailsComponent.startGuidedEntry`, déclenché par `ReservationsCreationService` juste après création si `viewport.isMobile()`) — Type → Résa → date de début → heure de début → date de fin → heure de fin, même principe que `ActivityFormComponent.startGuidedEntry` ; `DatePickerComponent` a gagné un `openPanel()` public (jusqu'ici seul `SelectComponent`/`TimePickerDialogComponent` l'avaient) pour permettre l'ouverture programmatique. Les champs spécifiques au type (lieu hôtel, compagnie du vol...) ne sont volontairement pas chaînés (trop hétérogènes d'un type à l'autre, voir le plan retenu) — laissés à l'édition manuelle juste en dessous
  - Deep-link bandeau réservation → onglet Réservations (`ReservationFocusService`) : ne dépliait/scrollait pas toujours la bonne carte — `ReservationsListComponent` ne retentait pas si `reservationCards()` (viewChildren) n'était pas encore à jour au moment du `afterNextRender` (juste après bascule d'onglet). Ajout d'une retry bornée (15 frames `requestAnimationFrame`), même forme que `DayScrollSyncService.focusActivityWhenReady` pour l'équivalent activité
  - Carte togglée par défaut en vue jour (mobile) : cause = la fonctionnalité "carte repliée pendant l'édition" (voir plus haut, décision produit de la retirer)
- **Suite retours utilisateur sur le chaînage réservation (2026-07-29)** :
  - Titre "Check-in"/"Check-out" (ou équivalent selon le type, voir `RESERVATION_TYPE_META`) ajouté en tête des panneaux `app-date-picker`/`app-time-picker-dialog` ouverts par le chaînage guidé — jusqu'ici rien n'indiquait si c'était la date/heure de début ou de fin qui s'affichait. `DatePickerComponent` a gagné un `label` input (même rôle que `SelectComponent.label`)
  - `TimePickerClockComponent` (cadran mobile) s'ouvrait sur l'heure ACTUELLE quand aucune heure n'était encore renseignée (`new Date()` par défaut dans le constructeur) — corrigé en `00:00`, même règle que partout ailleurs pour une heure non renseignée
  - Bouton "Ajouter" (réservation) qui ne recentrait plus sur la réservation créée : même cause que le deep-link ci-dessus (`ReservationsCreationService.createReservation`, un seul `afterNextRender` sans retry) — même correctif (retry bornée)
  - Double-clic sur "Ajouter" pendant la fenêtre entre la création et la localisation de la carte (`afterNextRender`) ouvrait un second draft/dialog en parallèle du premier au lieu de re-scroller vers la création en cours — `DayActivityCreationService`/`ReservationsCreationService` suivent désormais l'id en cours de création (`creatingInstanceId`/`creatingId`) et redirigent un clic pendant cette fenêtre vers un simple scroll sur la carte déjà en cours de création, plutôt que d'en démarrer une seconde
- Édition d'activité 100% par composants dédiés mobile : notes et prix/devise, les deux derniers champs édités inline, ont chacun désormais un drawer (`NotesFieldComponent`/`PriceFieldComponent`, `src/app/shared/components/notes-field/`, `price-field/`) — même bascule mobile/desktop que `TimePickerDialogComponent` (desktop : édition inline strictement inchangée ; mobile : trigger transparent qui ouvre le drawer avec le focus clavier posé immédiatement). Le champ prix (`{amount, currency}`) est passé d'un `FormGroup` imbriqué à un `FormControl` composite unique dans `ActivityFormComponent`/`ReservationDetailsComponent`. Appliqué aux deux formulaires (activité ET réservation, qui dupliquaient ces mêmes blocs). Chaînage de saisie guidée mobile de l'activité : la dernière étape (prix) ouvre désormais ce drawer au lieu de simplement focaliser l'input inline.
  - **Affinages UX (2026-07-29, suite retours utilisateur)** : les drawers ne sont plus plein écran — panneau centré (horizontalement ET verticalement, position par défaut de `DialogService`/CDK, jamais une `positionStrategy`/un `position:fixed` custom qui décentre le panneau, voir le commentaire "IMPORTANT" dans `field-edit-dialogs.scss`), taille de contenu plafonnée par `dvh` (`.app-price-dialog-panel` compact, `.app-wide-dialog-panel` partagée avec le tiroir titre — une seule ligne de note ou 2 suggestions ne prennent pas tout l'écran, ça grandit seulement si le contenu en a besoin, scroll interne au-delà). Pied de page uniforme "Annuler"/"OK" (même pattern que `TimePickerClockComponent`) sur les trois tiroirs (notes, prix, **et `TitleEditDialogComponent`, restructuré au passage** en header+champ+liste+pied plutôt que champ+OK sur une seule ligne en haut) : la saisie reste un brouillon local tant que le tiroir est ouvert, elle n'est appliquée qu'au clic sur "OK" (le prix n'est plus lié en live aux `FormControl` réels, `PriceEditDialogComponent` a désormais les siens). Notes : la hauteur suit le texte tapé (`TextareaDirective`, une ligne à vide) et le `<textarea>` scrolle nativement lui-même une fois sa propre `max-height` atteinte (une première tentative faisait scroller le panneau CDK à la main — annulée en boucle par le passage `height:'auto'` de `TextareaDirective` à chaque frappe, voir la doc de `NotesEditDialogComponent`). `title-edit-dialog.scss` (plein écran) supprimé, devenu mort.
  - **Fix** : le prix enregistré ne s'affichait pas sur le trigger mobile après rechargement de la page (le desktop, lui, était correct) — `PriceFieldComponent` dérivait l'affichage via `toSignal(control.valueChanges)`, qui ne s'émet jamais pour un `writeValue()` posé avec `{emitEvent:false}` (dont un simple écho du store au chargement) ; remplacé par des signals dédiés mis à jour explicitement à la fois dans `writeValue` et dans l'abonnement `valueChanges`.
- Layout desktop/paysage en vue jour : carte à gauche (sticky, pleine hauteur), activités à droite, barre des jours passée sticky juste sous toolbar+header au lieu de flotter en bas — s'applique au vrai desktop ET au mobile tenu en paysage (`ViewportService.isSplitLayout`, seuil `orientation: landscape` + largeur mini, pas juste un breakpoint de largeur). Chrome (toolbar+header+jours) jamais masqué au scroll quand la hauteur de viewport est suffisante (`ViewportService.isChromePinned`, desktop) ; sur mobile paysage (hauteur insuffisante), la barre des jours rejoint le groupe qui se masque au scroll avec toolbar+header, exactement comme ce dernier le faisait déjà seul (`TripChromeService`, nouveau `ChromeMode` : `'mobile' | 'split-hideable' | 'split-pinned'`). Grille multi-colonnes des activités et carte pliée dans l'onglet Général restent hors scope (déjà repoussées, voir plus haut/ci-dessous).
  - `day-panel` : timeline+carte et bandeau réservation+activités forment 2 colonnes VRAIMENT indépendantes en hauteur en layout scindé (`.day-panel-map-group`/`.day-panel-activities-group`, chacune sa propre colonne flex) — un premier jet en grille CSS partagée (`grid-template-areas`) forçait l'alignement des deux "lignes" entre colonnes, laissant un vide sous le bandeau réservation (souvent court) avant le début des activités ; écarté. Ordre mobile (bandeau, timeline, carte, activités) préservé sans dupliquer de markup : ces 2 groupes passent en `display:contents` en dessous du seuil scindé (leurs enfants redeviennent des enfants flex directs de `.day-panel-body`, réordonnés via `order`), et deviennent de vraies boîtes seulement en layout scindé. La colonne carte prend tout l'espace disponible (`flex:1 1 auto`), la colonne activités reste capée à une largeur de lecture raisonnable (`flex:0 1 28rem`) — l'inverse du premier jet, qui figeait la carte à une largeur fixe et laissait les activités s'étirer sur tout le reste. `align-items:stretch` (pas `flex-start`) sur les 2 colonnes : sans ça, la carte sticky "décroche" et défile hors champ dès que sa propre colonne (timeline+carte, courte) est épuisée, bien avant la fin d'une longue liste d'activités — l'étirement donne à la carte assez de "piste" pour rester collée tout le long. Espacement uniforme (`gap-1`/`gap-2`, mêmes valeurs que les utilitaires `layout-utilities.scss`) au lieu de marges ad hoc. Le `swiper-slide` ancêtre reste l'unique conteneur de scroll, donc `DayScrollSyncService` (offsets de cartes, snap, animation caméra) continue de fonctionner sans changement de mécanique, avec juste son "bouclier" de déclenchement (distance carte-liste) ramené à une marge minimale en layout scindé (la carte n'est plus empilée au-dessus de la liste, donc ne bloque plus rien en haut du scroll).
  - Hauteur de la carte en layout scindé : un premier jet forçait `.map-container` interne de `@angular/google-maps` via `::ng-deep`/`!important` (mauvaise pratique, écarté) — remplacé par une chaîne `flex`/`height:100%` bout en bout à travers des éléments qu'on possède (`.sticky-map` en hauteur concrète, `PanelComponent` a gagné un input `fillHeight` opt-in pour s'étirer pleine hauteur au lieu de se dimensionner sur son contenu, `<app-trip-day-map>` relaie la hauteur de son hôte), pour ne transmettre à `google-map` qu'un `[height]="'100%'"` normal via son API publique — piège rencontré : un sélecteur composé commençant par une classe du host (`.app-panel--fill-height .foo`) doit s'écrire `:host(.app-panel--fill-height) .foo` sous l'encapsulation émulée d'Angular, sinon le compilateur lui ajoute un attribut de scope qui ne matche jamais le host (qui ne porte que `_nghost-*`).
  - `.sticky-map`/bascule "Général" collaient à `top:0` : correct en mode mobile (le chrome finit de se masquer au scroll pile au moment où l'élément "arrive"), mais en `'split-pinned'` (desktop, chrome jamais masqué) ça les collait DERRIÈRE le chrome, en permanence invisibles une fois le scroll engagé. `TripChromeService.stickyContentTop` (nouveau, exposé en CSS via `--sticky-top-offset`) : `0` tant que le chrome peut se masquer, `contentTopOffset()` (juste sous le chrome) en `'split-pinned'` — tout élément sticky DANS le scrollport doit l'utiliser, jamais `0` en dur.
  - Bascule Activités/Réservations/Notes de l'onglet "Général" (`GeneralPanelComponent`, version desktop ≥768px) : n'était pas sticky, défilait hors champ avec le reste — épinglée en haut du scrollport avec le même mécanisme que `.sticky-map`.
  - **Suite retours utilisateur (2026-07-29)** : plafond de largeur retiré en layout scindé (`.day-panel-root { max-width: none; }` au lieu de `--nt-split-content-max-width`, token supprimé, désormais inutile) — il recréait exactement les bandes vides gauche/droite que la colonne carte `flex:1 1 auto` était censée éliminer sur grand écran. Colonne activités élargie (`28rem` → `32rem`). État vide (jour sans activité) recentré et replié en une seule colonne pleine largeur (plafonnée comme le mode empilé) au lieu de rester collé à gauche dans sa largeur de colonne étroite avec tout le reste de l'écran vide à droite. Gap-2 ajouté entre header voyage/barre des jours et entre barre des jours/contenu (`TripChromeService.tabsNavTop`/`contentTopOffset`, nouvelle constante `SPLIT_CHROME_GAP_PX`) — la barre collait pile sous le header et le contenu pile sous la barre. Toutes ces valeurs de gap (empilé/scindé) centralisées dans `--nt-layout-gap-stacked`/`--nt-layout-gap-split` (tokens.scss, mêmes valeurs que `.gap-1`/`.gap-2` dans layout-utilities.scss, qui les référencent désormais au lieu de dupliquer les literals) — plus aucun `0.25rem`/`0.5rem` en dur dans day-panel.component.scss. Carte floue/tuiles non rechargées au pan une fois agrandie : `DayScrollSyncService.attachMap` ne déclenchait `google.maps.event.trigger(map,'resize')` qu'une seule fois, juste après le déplacement DOM initial — jamais lors des redimensionnements CSS ultérieurs du même conteneur (fill-height, resize fenêtre...), donc l'instance continuait de peindre à son ancienne taille interne, juste étirée en CSS. Le `ResizeObserver` déjà en place (mesure de `stickyHeight`) déclenche désormais aussi ce `resize` à chaque changement de taille réel du conteneur.
  - **Suite retours utilisateur, 2e passe (2026-07-29)** : `mb-2` retiré de `DayReservationBannerComponent` — relicat d'avant l'introduction du `gap` uniforme sur `.day-panel-body`, il s'ajoutait par-dessus (0.5rem de plus), rendant l'écart bandeau→bloc suivant visiblement plus grand que les autres. Gap-1 ajouté entre header voyage et premier bloc de contenu en mode `'mobile'` (nouvelle constante `STACKED_CHROME_GAP_PX`, même principe que `SPLIT_CHROME_GAP_PX` pour le layout scindé) : il était totalement absent, contenu collé pile sous le header.
  - **Suite retours utilisateur, 3e passe (2026-07-29)** : gap interne entre cartes réservation du bandeau (`.day-reservation-banner`) posé en dur en `.gap-2` (utilitaire) quel que soit l'écran — remplacé par `--nt-layout-gap-stacked`/`--nt-layout-gap-split` comme le reste, gap-1 sur mobile désormais.
  - **Suite retours utilisateur, 4e passe (2026-07-29)** : encore 2 relicats du même genre — `padding-top: 0.5rem` sur `.sticky-map` (mobile) s'ajoutait par-dessus le `gap` entre la timeline et la carte, gonflant spécifiquement cet écart à plus que gap-1 (retiré, l'espacement avec l'élément précédent est déjà géré par le `gap` de `.day-panel-body`) ; `mb-2` sur `<app-toolbar>` (`TripsComponent`) créait un écart entre la toolbar et le header voyage jamais voulu par le système de gap actuel (retiré, les deux sont désormais flush). Carte à moitié vide sur mobile (jour avec activités très dispersées) : investigué une 2e fois sans réussir à reproduire — reste ouvert, voir la section "Bugs / fixes" pour les détails demandés à l'utilisateur pour la retrouver.
  - **Suite retours utilisateur, 5e passe (2026-07-29)** : correction de la passe précédente — toolbar/header voyage ne devaient pas devenir flush (0 gap), mais suivre le même gap-1 empilé/gap-2 scindé que le reste (`TripChromeService.headerTop`, nouveau, réutilise `STACKED_CHROME_GAP_PX`/`SPLIT_CHROME_GAP_PX`). Barre des jours : insets gauche/droite (`left-0 right-0`, bords collés à l'écran) remplacés par le même 0.5rem que `.app-trip-header-fixed`/`.tab-content-wrapper` (nouvelle classe `.app-trip-tabs-nav-fixed`), seul élément fixe du groupe à ne pas le reproduire jusqu'ici. Double écart bandeau réservation → carte/activités quand le bandeau est vide (aucune réservation ce jour-là) : `.day-panel-body`/`.day-panel-activities-group` posent un `gap` entre TOUS leurs enfants directs, y compris un enfant "vide" (0 hauteur mais toujours présent dans le flux flex) — `DayReservationBannerComponent` passe désormais en `display:none` (host binding) plutôt que juste visuellement vide quand `occurrences().length === 0`, pour ne plus consommer de `gap` du tout dans ce cas.
  - Carte (mobile, vue jour) : rendu partiellement vide/blanc sur une partie de la largeur signalé par l'utilisateur (jour avec activités très dispersées géographiquement, zoom "vue d'ensemble" très arrière). Investigué le 2026-07-29 (Playwright, container recréé avec des lieux Google dispersés Europe/Asie/Océanie) : le dimensionnement du conteneur `.sticky-map`/`google-map` est confirmé correct (largeur interne = largeur du conteneur, le correctif `resize` trigger de `DayScrollSyncService` fonctionne), impossible de reproduire le rendu à moitié vide tel que décrit. Hypothèse non confirmée : édge case Google Maps (tuiles raster, fallback vector→raster déjà observé dans cet environnement) à zoom très arrière combiné à un centre/bbox calculé par `computeOverviewCamera` (trip-day-map.component.ts) qui ne couvrirait pas toute la largeur d'un conteneur mobile étroit et haut. À reproduire avec plus de détail avant de corriger : arrive-t-il systématiquement sur ce jour précis ? Persiste-t-il après quelques secondes (tuiles encore en chargement ?) ou après une interaction (pan/zoom) ?
- Carte dans le pool d'activités de l'onglet "Général" : repliée par défaut sur mobile, dépliée et positionnée en colonne latérale sticky sur desktop (même mécanique de layout scindé que la vue jour), avec le suivi caméra au scroll identique à un jour — recalculé quel que soit le mode de tri actif ("Chronologie"/"Ville") ou le filtre de recherche. Réutilise l'instance UNIQUE de carte partagée avec la vue jour (`TripDayMapHostService`, désormais avec un `currentOwner: 'day' | 'general'`) et le même moteur de scroll-sync (`DayScrollSyncService`, une instance dédiée par montage du sous-onglet Activités). Fold state volontairement indépendant de la vue jour (`GeneralMapPanelService`, nouveau — la vue jour reste toujours dépliée par défaut, inchangée) : son défaut suit le viewport (`linkedSignal` sur `!ViewportService.isSplitLayout()`), tout en restant modifiable manuellement entre deux franchissements de seuil desktop/mobile.
- **Lot UX/bugs du 2026-07-30** :
  - Prix à 0 affichait "Ajouter un prix" au lieu de "0" (trigger mobile, `PriceFieldComponent.displayAmount`) : `@if` sur une valeur potentiellement `0` est toujours faux en template Angular — condition changée en `!== null`.
  - Anneau de focus clavier/clic thémé posé globalement (`:focus-visible` dans `reset.scss`) à la place de l'anneau bleu par défaut du navigateur sur les éléments custom `role="button"`/`tabindex` (ex. cartes de `accueil-trip`) : les tokens `--nt-focus-ring-*` existaient déjà dans `tokens.scss` mais n'étaient utilisés que pour le `box-shadow` des champs de formulaire, jamais appliqués aux éléments cliquables custom.
  - "Devise :" ajouté en libellé visible devant le sélecteur du header voyage (`TripHeaderComponent`) — le `label` de `app-select` ne sert qu'au titre du tiroir mobile, jamais affiché inline.
  - Barre flottante mobile Activités/Réservations/Notes (`GeneralSubTabBarComponent`) : animation d'apparition (glisse depuis le bas + fondu) à chaque montage/démontage du `@if` — box-shadow et centrage étaient déjà en place.
  - Préremplissage fin ← début sur le formulaire Logistique (`LogisticDetailsComponent`) : la date de fin et l'heure de fin se calquent désormais sur le début dès sa sélection, tant que la fin n'a pas déjà été éditée indépendamment (même principe que la durée par défaut à 0 de `ActivityFormComponent`, qui avait déjà ce comportement côté activités).
  - Date de début d'un élément Logistique préremplie à la création : jour cliqué si créé depuis le menu "Ajouter" d'un jour (`DayLogisticQuickAddService.create`, nouveau paramètre `dayDate`), sinon le premier jour du voyage si créé depuis le "+" du sous-onglet Logistique (`LogisticsCreationService.tripStartDate`) — jamais l'heure, qui reste vide jusqu'à saisie explicite (voir la règle déjà en place, "Réservations, dates jamais préremplies").
  - Titre Vol/Train (`LogisticHeaderComponent`) : texte simple en lecture seule (`hasComputedTitle`) au lieu d'un champ éditable en première zone de saisie du header, pour ne plus laisser croire qu'il faut le renseigner alors qu'il est calculé par la cinématique guidée — crayon dédié (`openComputedTitleEditDialog`, réutilise `SimpleTextEntryDialogComponent`) pour l'écraser manuellement si besoin. Logement (déjà son propre dialog Google-aware) et Location voiture/Autre (texte libre inchangé) non concernés.
  - Ville de retour d'un vol : décision produit, pas de traitement dédié (voir plan d'exécution en tête de fichier) — un aller-retour reste 2 éléments "Vol" indépendants.
  - Items du "🔧 À faire" reconfirmés déjà résolus par des correctifs antérieurs (pas de nouveau code, juste vérification) : padding case à cocher/drag-and-drop mobile logistique (le drag-and-drop manuel des réservations/logistique a été retiré, voir la décision "Tri des réservations" du 2026-07-28 — plus de poignée du tout à espacer), clic sur bannière hôtel/transport qui ne recentrait pas le scroll (déjà couvert par le retry borné de `LogisticsListComponent.focusCardWhenReady`, `card.element.scrollIntoView(...)`), calendrier absent en vue ordinateur (le desktop de `app-date-picker` a déjà un champ texte ET un bouton icône séparé pour ouvrir le calendrier), double-clic sur "Ajouter" qui cassait la carte de création (déjà gardé par `creatingInstanceId`/`creatingId`, activités ET logistique, mobile ET desktop).
- **Suite retours utilisateur (2026-07-30)** :
  - Anneau de focus : trop générique sur les cartes cliquables pleine largeur d'`accueil-trip.component.html` (carré autour de toute la carte) — remplacé sur cet écran par un style dédié (`accueil-trip.component.scss`, jusque-là orphelin/non branché, `styleUrl` manquant sur le composant) : `outline:none` + le titre du voyage ET la flèche changent de couleur, l'anneau global (`reset.scss`) reste le comportement par défaut ailleurs. **Persistant sur mobile malgré ce correctif** (retour utilisateur, capture à l'appui) : cause distincte, `-webkit-tap-highlight-color` — le flash de tap bleu par défaut de WebKit/Blink mobile sur tout élément cliquable, un calque de rendu à PART de `outline`/`:focus-visible` que ce dernier ne supprime pas. Neutralisé globalement (`html { -webkit-tap-highlight-color: transparent; }` dans `reset.scss`) plutôt que juste sur la carte de voyage, pour éviter la même resurgence ailleurs. **Coloration titre/flèche encore absente malgré ça** (2e retour utilisateur) : la règle ciblait `:focus-visible`, qui ne matche PAS un clic souris/tap tactile sur un `div[tabindex]` (heuristique Chromium — seul un vrai focus clavier le déclenche), donc ne s'appliquait jamais au clic réel malgré une vérification Playwright trompeuse (`.focus()` programmatique, qui matche `:focus-visible` contrairement à un clic). Corrigé en `:focus` tout court ; reconfirmé cette fois avec un clic RÉEL simulé (`page.mouse.down()`, pas `.focus()`) : couleur du titre et de la flèche = `--nt-primary-color` au clic.
  - "Devise :" sur la même ligne que les dates de début/fin dans le header voyage (position d'origine, juste le libellé "Devise :" en plus) — ajustement final de l'alignement fait directement par l'utilisateur (`justify-content-between` sur la ligne, span du libellé sans `nt-text-muted`).
  - Barre flottante mobile Activités/Réservations/Notes collée à la barre des jours juste en dessous (même fond `--nt-content-background`, coins bas non arrondis) : les deux se lisent comme une seule surface continue au lieu de 2 pastilles séparées par un interstice laissant voir le contenu défiler dessous.
  - **Régression confirmée** : créer un Vol ou un Train depuis le "+" flottant du sous-onglet Logistique (pas depuis le menu "Ajouter" d'un jour, qui connaît déjà le type) fermait tout seul le dialog "Numéro de vol"/"Ville de départ" de la cinématique guidée juste après avoir choisi le type. Cause tracée via Playwright (instrumentation temporaire, retirée) : `LogisticsListComponent` affichait le tri "Type" (mode par défaut) avec un `@for` PAR SECTION (une boucle imbriquée par type) — dès que le type passe de `'other'` à `'flight'`/`'train'`, l'élément change de section ; si la section de destination n'existait pas l'instant d'avant (aucun élément de ce type), Angular détruit la carte dans l'ANCIENNE boucle et en recrée une NOUVELLE dans la boucle qui vient d'apparaître, même trackée par id (le tracking ne dédoublonne qu'au sein d'une même boucle) — ça coupait net la cinématique en cours (tout dialog CDK ouvert via le `ViewContainerRef` de l'ancienne instance est détruit avec elle). Pas une régression de ce lot à proprement parler (mécanique déjà présente depuis la refonte Logistique), simplement jamais déclenchée avant faute d'avoir testé ce chemin précis (le menu "Ajouter" d'un jour, qui connaît le type à l'avance, n'est pas concerné). Corrigé en remplaçant les `@for` imbriqués par UNE SEULE boucle plate (`LogisticsListComponent.typeRows`, en-têtes de section + cartes dans le même tableau) : Angular détecte alors un simple déplacement de la même entité trackée et déplace le nœud/l'instance au lieu de la détruire.
- Uniformisation des 8 dialogs de l'app (croix/titre/OK) : audit complet montrant que les titres en gras (`font-weight: 600`) et l'absence d'"OK" dupliqué à côté d'un champ de saisie étaient déjà cohérents partout ; seule vraie incohérence trouvée, la croix de fermeture à gauche du titre sur les 4 tiroirs mobile "champ dédié" (`TitleEditDialogComponent`, `NotesEditDialogComponent`, `PriceEditDialogComponent`, `SimpleTextEntryDialogComponent`) alors qu'elle est à droite sur les dialogs bâtis sur `<app-dialog-frame>` (`ConfirmDialogComponent`, `CollaboratorsDialogComponent`, `TimePickerClockComponent`) — décision utilisateur le 2026-07-31 : croix à droite partout (alignée sur `app-dialog-frame`), les 4 tiroirs mobile mis à jour en conséquence (`field-edit-dialogs.scss` mis à jour pour refléter le nouveau choix).
- Titres des tiroirs Heure (`TimePickerClockComponent`) raccourcis pour matcher le registre concis des autres dialogs (Titre, Notes, Prix) : "Sélectionner l'heure de début/de fin/la durée" → "Heure de début"/"Heure de fin"/"Durée" (`ActivityFormComponent`) ; fallback générique (aucun `label` fourni) "Sélectionner une heure/une durée" → "Heure"/"Durée" (`TimePickerClockComponent`). Les titres déjà concis de `LogisticDetailsComponent` ("Heure — Check-in", "Heure — Départ"...) n'avaient pas besoin de changement.
- Centrage du "0" dans la dialog Prix (`PriceEditDialogComponent`) : deux corrections distinctes. Vertical — le chiffre paraissait décalé vers le haut de sa zone de saisie malgré un padding haut/bas symétrique, à cause de `line-height: normal` (hérité, jamais fixé pour ce champ) qui réserve un espace sous la ligne de base pour d'éventuelles descendantes (g, y, p...) qu'un montant n'utilise jamais — visible seulement à `font-size: 2rem` ; corrigé avec `line-height: 1`. Horizontal — la zone de saisie elle-même (pas son texte) était collée à gauche de la popup : `app-input-number` (l'hôte du `<input>`) est en `display:block; width:100%`, donc `align-items:center` du body n'a aucun effet visible sur lui (déjà pleine largeur) ; c'est l'`<input>` interne, plus étroit (`width: min(16rem, 100%)`) mais laissé en `inline-block` par défaut, qu'il fallait recentrer — corrigé avec `display:block; margin:0 auto` sur `.price-edit-dialog__amount`.
- Scroll du tiroir "Titre" (recherche Google) et du tiroir "Notes" : la popup ENTIÈRE pouvait défiler (`overflow-y:auto` posé sur `.app-wide-dialog-panel`, field-edit-dialogs.scss) en plus du scroll interne de la liste/du `<textarea>`, cachant potentiellement le titre et le pied Annuler/OK (jamais garantis au-dessus du clavier). Cause racine tracée via inspection runtime (Playwright, `getComputedStyle`) : `<cdk-dialog-container>` (CDK, injecté entre le panneau et le composant ouvert) est en `display:block`, pas flex — le composant enfant grandissait donc à la taille de son contenu au lieu de recevoir la hauteur réellement disponible, débordant silencieusement (`overflow:visible` par défaut sur ce conteneur CDK). Corrigé en repassant ce conteneur en `flex column` (`dialog.scss`, global — bénéficie aussi aux dialogs `<app-dialog-frame>` en cas de contenu long, ex. `CollaboratorsDialogComponent`) + `flex:1 1 auto; min-height:0;` sur le composant hôte de `TitleEditDialogComponent`/`NotesEditDialogComponent` (remplace les anciens calculs `max-height: calc(100dvh - Nrem)` fragiles par un flex qui absorbe la hauteur réellement disponible) ; suppression du `overflow-y:auto` en trop sur `.app-wide-dialog-panel`. Vérifié avec un texte de note très long (Playwright + inspection `getBoundingClientRect`) : header/titre et pied Annuler/OK restent fixes, seul le contenu défile.
- Confirmation `ChangeDetectionStrategy.OnPush` (Angular 22, zoneless) : `OnPush` n'est jamais implicite, la valeur par défaut de la métadonnée `changeDetection` reste `Default` sauf déclaration explicite — vérifié que les 75 fichiers `@Component` du repo le déclarent bien tous explicitement, rien à corriger, ne jamais le retirer.
- **Refonte "Réservation" → "Logistique"** : renommage complet du concept dans le code (dossiers/fichiers/types/services `Reservation*` → `Logistic*`, champ Firestore `reservations` → `logistics`, `type: 'hotel'` → `'logement'`, libellé de l'onglet et sous-onglet "Logistique" — pas de script de migration, décision utilisateur, seules quelques réservations de test existaient) ; nouveau type "Train" (`departurePlace`/`arrivalPlace`, labellisé "Départ"/"Arrivée") ; 5 couleurs d'identité par type (`--nt-logistic-*` dans tokens.scss, appliquées à l'icône du header de carte, à la bannière jour et aux en-têtes de section du tri "Type") ; nouvel endpoint Cloud Function `/vols/{n}/lookup` (AeroDataBox, `mapFlightLookup`) dédié à la cinématique de création d'un vol (compagnie/aéroports/horaires prévus), distinct de l'endpoint `/status` existant (suivi temps réel inchangé) ; cinématiques de création guidée entièrement revues, désormais démarrées par le choix du TYPE (menu `MenuComponent`/`app-select` selon le cas) avant tout autre champ, chacune spécifique : Logement (nom via `TitleEditDialogComponent` réutilisé — Google ou texte libre + adresse en repli — puis Résa/dates/prix, l'ancien champ "Hôtel" dédié supprimé, doublon avec le titre désormais lui-même "Google-aware" pour ce type via `LogisticHeaderComponent.isLogement`), Vol (numéro + date de départ → lookup AeroDataBox, titre auto `Vol (N) - dép - arr`, repli manuel si le vol n'est pas trouvé), Location voiture (Résa/dates/nom du loueur, titre auto), Train (Résa/ville départ/ville arrivée/dates), Autre (titre libre) — toutes terminées par une étape Prix (`PriceFieldComponent.openDialog`) ; nouveau composant générique `SimpleTextEntryDialogComponent` (champ texte + OK, sans recherche Google) pour les quelques champs texte de ces cinématiques (n° de vol, loueur) ; bouton "Ajouter" d'un jour ouvrant désormais un choix à 6 entrées (Activité + les 5 types logistiques, `TripDetailComponent.dayAddMenuItems`, `MenuComponent.toggleAt` nouveau pour ancrer un menu sur un élément hors de son propre template) — un choix logistique crée l'élément puis bascule sur l'onglet Général/sous-onglet Logistique et enchaîne la cinématique guidée (type déjà connu, `LogisticFocusService` étendu d'un flag `startGuided`) via `DayLogisticQuickAddService` ; sous-onglet Logistique doté d'un tri "Chronologie"/"Type" (regroupement par type, ordre fixe, sections colorées) + recherche par titre, même pattern que l'onglet Activités. L'amélioration visuelle du sous-menu ("plus fondu"/accordéon) reste volontairement hors de ce lot, voir "🔧 À faire".
- **Lot UX/Interactions du 2026-07-31** (7 items non "non prioritaire" de la section, voir décisions en tête de fichier) :
  - Carte du pool d'activités (onglet Général) : caméra entièrement décorrélée du scroll (contrairement à la vue jour, `DayScrollSyncService`/`cameraFollowEnabled`, inchangée — gate ajoutée mais `true` par défaut). Nouveau service dédié `GeneralMapCinematicService` (fourni par `TripActivitiesComponent`), machine à états : `idle-at-overview` (repos, vue d'ensemble) → `touring` (promenade lente point à point après 2s d'inactivité, réutilise `TripDayMapComponent.followScroll` tel quel — même algo/easing que l'ancien suivi en direct — mais piloté par le TEMPS via `requestAnimationFrame` plutôt que par le scroll) → `returning-to-overview` (retour en douceur à la vue d'ensemble, fin de tour ou action "page" hors carte — scroll du pool, clic sur une carte, recherche, tri) → retour à `idle-at-overview` ; 4ᵉ état `paused-by-map` (action DIRECTEMENT sur la carte — drag, molette/pincement, clic marqueur) qui fige l'automatisme SANS jamais toucher la caméra (l'utilisateur garde la main), `tourPointIndex` retient où en était le tour pour reprendre exactement là après 2s d'inactivité (pas de retour à la vue d'ensemble, pas de redémarrage au point 1). `TripDayMapComponent` gagne 2 méthodes publiques additives (`getCameraState`, `moveCameraTo`) + `computeOverviewCamera` passée en public, pour permettre ce tween depuis un état caméra arbitraire — aucune méthode existante modifiée, vue jour inchangée. Wake Lock API (feature-détectée, échec silencieux) tenue pendant les animations pour empêcher la mise en veille de l'écran.
  - Correctif : drag de la scrollbar desktop (vue jour) ne faisait pas suivre la caméra comme la molette — `DayScrollSyncService.onSlideScroll` (écouteur `scroll` natif) n'appelait jamais `wakeLoop()` contrairement à `wheel`/`touch`, donc si la boucle rAF était déjà retombée idle un drag de scrollbar ne la relançait pas ; même correctif que `TripDaySwiperComponent.onSlideScroll` (chrome) appliqué ici.
  - Contour des groupes ville/jour/à-assigner du pool Général (`app-panel`) légèrement plus épais que les cartes d'activité (qui partagent le même composant) : nouvel input `thickBorder` sur `PanelComponent` (`--nt-border-width-thick` au lieu du hairline par défaut), posé uniquement sur les 3 conteneurs de groupe de `TripActivitiesComponent`.
  - Bascule "Lieu / Chronologie" (Activités) et "Chronologie / Type" (Logistique), variant `'subtle'` de `SelectButtonComponent` (seul usage de ce variant dans le repo) : rendu "switch" avec une vraie pastille glissante (`.app-select-button__thumb`, translatée/animée derrière l'option active) plutôt qu'un simple `box-shadow` sur l'item actif.
  - Date picker desktop (`app-date-picker`) : icône calendrier déplacée DANS le champ (décorative, `pointer-events:none`) au lieu d'un bouton séparé à côté ; un clic n'importe où dans le champ ouvre désormais le panneau (`onFieldClick`, jamais ne le ferme) — l'ancienne séparation stricte input/bouton (documentée dans la classe) visait à éviter un risque marginal de course focus/clic sur le panneau CDK, accepté ici pour matcher le comportement demandé.
  - Logistique (`LogisticHeaderComponent`) : titre uniformément en lecture seule + crayon dédié sur les 5 types (auparavant Logement = titre entier cliquable, Location voiture/Autre = champ texte inline édité en direct) — Logement garde son dialog Google-aware (`openLogementTitleDialog`) derrière le crayon, les autres types partagent un dialog texte simple (`openTitleTextDialog`, ex-`openComputedTitleEditDialog` renommé). Champ `<input>` inline et handlers associés (`onTitleInput`/`onTitleBlur`/`onTitleFocus`) supprimés, devenus inutiles.
  - Bouton "+" uniforme partout (menu à 7 entrées — Activité, 5 types Logistique, Note — désormais ouvert aussi bien depuis un jour que depuis l'onglet Général, qui créait auparavant directement l'élément du sous-onglet actif sans menu) : "Activité" depuis Général bascule le sous-onglet Activités puis crée une activité de pool (`GeneralCreationTarget.createActivity`, renommage de l'ancien `trigger` contextuel, devenu inutile) ; nouvel entrée "Note" via `NotesFocusService` (même schéma de navigation croisée à 3 niveaux que `LogisticFocusService` — bascule d'onglet, bascule de sous-onglet, création + centrage + focus par `NotesComponent.addItem`), atteignable depuis n'importe quel onglet.
  - **Suite retours utilisateur (2026-07-31)** :
    - Pastille glissante de `SelectButtonComponent` (variant `'subtle'`) visuellement désalignée : le 1er jet dimensionnait/positionnait la pastille en pourcentage fixe (`100% / options().length`, `translateX(index * 100%)`), en supposant 2 options de largeur ÉGALE — faux, les items sont `flex: 0 0 auto` (largeur dictée par leur contenu, icône+libellé, ex. "Lieu" bien plus court que "Chronologie"). Corrigé par mesure DOM réelle de l'item actif (`offsetLeft`/`offsetWidth` via `afterRenderEffect` + `viewChildren('itemEl')`, nouveau signal `thumbRect`), la pastille suit désormais exactement les bords du bouton actif quelle que soit sa largeur.
    - Algo caméra "cinématique" (`TripDayMapComponent.followScroll`, utilisé À LA FOIS par le suivi en direct de la vue jour ET le tour du pool Général) : `easeInOutQuad` remplacé par `easeInOutCubic` (ralentissement plus marqué à l'approche de chaque point) ; `computeCinematicZoom` réécrit — l'ancienne formule log-distance plafonnait le dézoom à `MAX_ZOOM_DROP = 1.8` niveaux, largement insuffisant sur de longues distances (nord/sud de la France, et pire France/Chine, restaient beaucoup trop zoomés pendant le déplacement) ; remplacée par le même calcul bbox→zoom que `computeOverviewCamera`/`getBoundsZoomLevel` (zoom réel nécessaire pour que les 2 points tiennent dans le cadre), sans plafond arbitraire.
    - Cinématique du pool Général (`GeneralMapCinematicService`) spécifiquement : déplacement caméra ralenti (`SEGMENT_MS`/`RETURN_MS` × 3, "au moins 3 fois plus lent" demandé) + légère pause (`DWELL_MS` = 700ms) sur chaque point atteint avant d'enchaîner vers le suivant.
  - **2e passe retours utilisateur (2026-07-31)** :
    - Dézoom cinématique perçu comme désynchronisé du déplacement (la caméra semblait avancer sans reculer, puis reculer d'un coup, puis "arriver" zoomée alors qu'elle se déplaçait encore) : cause probable, l'ancienne enveloppe de zoom (`4*t*(1-t)`, parabole étroite avec un pic ponctuel à t=0.5) — remplacée par `TripDayMapComponent.zoomEnvelope` (rampe smoothstep sur les 30% premiers/derniers, plateau au max de dézoom sur toute la partie centrale du trajet), strictement synchronisée sur le même `t` que la position (`followScroll`) : le dézoom démarre dès le départ du point et le rezoom se termine pile à l'arrivée.
    - Bascule "switch" (`SelectButtonComponent`, variant `'subtle'`) : contour individuel sur chaque bouton retiré (`border: none` explicite), remplacé par un contour unique posé sur le CONTENEUR (`--nt-content-border-color`, hairline) qui enveloppe les 2 boutons, sous la pastille glissante — plus proche de la référence fournie par l'utilisateur.
  - **3e passe retours utilisateur (2026-07-31)** :
    - Dézoom cinématique toujours perçu comme désynchronisé (surtout au-delà de 50km) malgré la rampe/plateau de la 2e passe : cause réelle — la FORME de l'enveloppe (parabole puis rampe+plateau) restait différente de celle du déplacement (linéaire en `t`), même en partageant le même `t` en entrée ; un plateau en particulier fige le zoom au max pendant que la position, elle, continue de bouger tout du long. Remplacée par `TripDayMapComponent.zoomEnvelope`, une simple "tente" linéaire (`1 - |2t-1|`) : à X% du trajet, le dézoom est exactement à sa valeur proportionnelle, quelle que soit la distance — dézoom-zoom prend littéralement "autant de temps que le déplacement lat/lng" (demande explicite).
    - Saut (sans transition) entre la vue d'ensemble et le 1er point au démarrage d'un tour du pool Général : `GeneralMapCinematicService.onIdleTimeout` appelait directement `runSegment` au point 0, qui positionne la caméra INSTANTANÉMENT dessus à `t=0`. Nouvelle étape `enterTour` (uniquement au tout premier départ depuis `idle-at-overview`, pas lors d'une reprise après `paused-by-map`) : réutilise `TripDayMapComponent.followFromOverview` pilotée par le temps (même moteur que les segments) pour transitionner en douceur vue d'ensemble → 1er point avant d'enchaîner le tour.
    - Bascule "switch" : le contour restait peu visible côté pastille primary (teal), lisible seulement côté bouton non sélectionné — donnant l'impression à tort de n'entourer qu'un seul bouton. Contour épaissi (`--nt-border-width-thick` au lieu du hairline) et recoloré avec `--nt-form-field-border-color` (même token que le variant `'solid'`, plus contrasté que `--nt-content-border-color`, pensé pour un pourtour de contrôle réellement visible) — vérifié par mesure DOM (`getComputedStyle`/`boundingBox`) : contour symétrique des 2 côtés, capture d'écran confirmant un rendu visible sur tout le pourtour.
  - **4e passe retours utilisateur (2026-07-31, schémas fournis par l'utilisateur)** :
    - Dézoom cinématique : la tente linéaire de la 3e passe (`1 - |2t-1|`) partage bien le même `t` que la position, mais dessine un pic ANGULEUX (pente qui s'inverse instantanément à mi-course) — confirmé par schéma utilisateur, perçu comme un "saut" au sommet plutôt qu'une vraie synchronisation. Ce qui était demandé (2e schéma) : une courbe LISSE en cloche, sans angle — retour à la parabole d'origine `4t(1-t)` (dérivée nulle au sommet, transition douce), en conservant le calcul de magnitude sans plafond de la 1re passe (le vrai bug initial de désynchronisation). `zoomEnvelope` documente cet aller-retour pour ne pas y revenir sans relire ce contexte.
    - Bascule "switch" : contour désormais bien visible des 2 côtés (3e passe), mais marge quasi inexistante entre le pourtour et la pastille colorée — augmentée (`padding`/inset de la pastille 0.2rem → 0.35rem) pour un pourtour sombre "un peu plus large que le bouton coloré" (schéma utilisateur), plus proche de la référence initiale (pilule colorée nettement plus petite que son conteneur).
  - **5e passe retours utilisateur (2026-07-31)** — utilisateur toujours "aucun changement visible" après un hard refresh confirmé : vérifié en direct sur `ng serve` (contexte navigateur neuf, `getComputedStyle`/`getBoundingClientRect`) que le code livré s'applique bien tel quel (padding/bordure mesurés symétriques des 2 côtés, ~8px de marge) — le code n'était donc pas en cause pour la géométrie. 2 pistes creusées en conséquence :
    - **Cause probable, trouvée, du dézoom "pas du tout une parabole"** : `TripDayMapComponent.mapOptions` ne posait jamais `isFractionalZoomEnabled`. Cette carte bascule en rendu RASTER dans cet environnement (voir le warning console "Falling back to Raster", déjà noté dans "Warnings de dépréciation Google Maps") — et le zoom fractionnaire est désactivé PAR DÉFAUT sur une carte raster (activé par défaut seulement en vectoriel) : chaque valeur de zoom non entière calculée (`followScroll`/`computeCinematicZoom`) était donc silencieusement ARRONDIE par l'API Google Maps avant rendu, réduisant n'importe quelle courbe continue (parabole comprise, quelle que soit sa forme) à une poignée de sauts discrets entre niveaux de zoom entiers — cohérent avec CHAQUE round précédent de retours ("saut", "pas une parabole du tout"), indépendamment de la formule JS utilisée en amont. `isFractionalZoomEnabled: true` ajouté explicitement.
    - Bascule "switch" : géométrie confirmée correcte et symétrique par mesure, mais contour toujours jugé peu visible côté pastille primary (fort contraste teal qui "absorbe" perceptivement un simple liseré fin juste à côté) — `box-shadow: var(--nt-overlay-popover-shadow)` ajoutée sur le conteneur (même token/même technique que "carte épinglée... ombre portée pour séparer visuellement", déjà en place ailleurs dans l'app) pour que toute la pilule se lise comme un objet encadré, plutôt que de compter uniquement sur le contraste de couleur du contour.
  - **6e passe retours utilisateur (2026-07-31) — cause racine du switch enfin trouvée** (via l'inspecteur navigateur de l'utilisateur) : la règle de base `.app-select-button__item` (pensée pour le variant `'solid'`) pose `background: var(--nt-form-field-background)`, jamais réinitialisée pour le variant `'subtle'` — CHAQUE item (pas seulement l'actif) peignait donc sa propre couleur de fond opaque (quasi noire en dark theme), masquant le fond partagé du conteneur sauf dans le mince interstice padding/gap. D'où l'impression tenace de "2 boîtes séparées" malgré tous les correctifs précédents (contour, épaisseur, marge, ombre) — aucun n'y pouvait rien puisque le vrai problème était que les items eux-mêmes recouvraient le fond commun. `background: transparent` ajouté sur `:host(.app-select-button--subtle) .app-select-button__item` : une seule pilule visuellement unifiée désormais (vérifié par capture d'écran, Lieu ET Chronologie actifs).
  - **7e passe retours utilisateur (2026-07-31) — diagnostic précis du zoom sur longue distance** (Bruxelles -> Hôtel Montmartre ~300km "catastrophique", vs Pigalle -> Naan Nation proche "parfait") : la description exacte du symptôme (déplacement horizontal rapide SANS zoom, puis dézoom/rezoom très rapide sur place, puis re-déplacement rapide) pointait vers un problème de RENDU (tuiles Google Maps qui ne suivent pas), pas de courbe mathématique — une durée de transition FIXE (`SEGMENT_MS`/`RETURN_MS`), identique quelle que soit l'ampleur du dézoom, ne laisse pas le temps aux tuiles de charger pour un dézoom de plusieurs niveaux (raster, voir le fix `isFractionalZoomEnabled` de la 5e passe), d'où un rendu qui semble "sauter" entre phases distinctes. Deux correctifs combinés :
    - `TripDayMapComponent.estimateZoomDrop` (extrait de `computeCinematicZoom`, rendu public) replafonné à `MAX_ZOOM_DROP = 6` (l'ancien retrait total du plafond en 1re passe s'est révélé "un tout petit peu trop" sur de très longues distances, retour utilisateur) — encore bien plus généreux que l'origine (1.8).
    - `GeneralMapCinematicService` (`enterTour`/`runSegment`/`returnToOverview`) : la durée de chaque transition n'est plus fixe, elle s'allonge proportionnellement à l'amplitude du dézoom à parcourir (`MS_PER_ZOOM_LEVEL` par niveau, en plus de la durée de base) — un petit trajet garde exactement la durée déjà jugée "parfaite", un gros dézoom obtient le temps nécessaire pour que le rendu suive.
    - Limite connue : ce correctif de durée ne s'applique qu'au pool Général (durée pilotée dans le temps) — la vue jour (caméra pilotée par la position de scroll, pas une durée) profite du replafonnement de `estimateZoomDrop` mais pas de l'allongement de durée ; à confirmer si le symptôme y persiste malgré ça.
  - **8e passe retours utilisateur (2026-07-31)** :
    - Toujours "la même sensation" en vue jour (scroll) malgré la 7e passe (qui ne pouvait rien y faire, voir sa limite connue) — demande explicite cette fois : accélérer le dézoom au DÉPART et à l'ARRIVÉE du point, le ralentir/figer au MILIEU (parabole `4t(1-t)` : l'inverse, le zoom change le plus vite pile au milieu et à peine aux 2 bouts). `zoomEnvelope` réécrite en rampe (`smoothstep`) + PALIER central (dézoom quasi figé sur la partie centrale de la course), et surtout PILOTÉE PAR `t` BRUT plutôt que par `eased` (`followScroll` passe désormais `clampedT`, pas `eased`, à `computeCinematicZoom`) : le rythme du zoom est totalement DÉCOUPLÉ de celui du déplacement (qui garde son propre `easeInOutCubic`), ce qui s'applique aussi bien à la vue jour (scroll) qu'au pool (temps) — contrairement au correctif de durée de la 7e passe, celui-ci n'est pas limité au pool.
    - Bascule "switch" : 3e couche ajoutée explicitement demandée — `.app-select-button__track` (nouvel élément, `background: var(--nt-form-field-background)`), inséré entre le conteneur (`--nt-secondary-bg`, "zone grise" qui englobe tout) et la pastille (couleur primary, bouton sélectionné) : la piste remplit tout le contenu (les 2 boutons) en restant visuellement DANS la marge de la zone grise. Vérifié par capture d'écran dans les 2 thèmes (contraste net en clair — piste blanche sur fond gris pâle — plus subtil mais présent en sombre, la piste `--nt-surface-950` restant plus foncée que le conteneur `--nt-surface-800`).
  - **9e passe retours utilisateur (2026-07-31)** :
    - Rampe+palier de la 8e passe jugée "trop carrée" (schéma utilisateur — palier plat perçu comme un dézoom/rezoom figé plutôt qu'une vraie courbe) : retour à une parabole lisse `4t(1-t)` (dérivée nulle SEULEMENT au sommet, pas de segment plat), en conservant le vrai correctif de la 8e passe (pilotée par `t` BRUT, pas `eased` — c'était ÇA qui manquait dans la toute première parabole testée plusieurs passes plus tôt, pas la forme de la courbe elle-même).
    - Transition fin de tour (dernier point -> vue d'ensemble) perçue comme "recentrage PUIS dézoom en séquence" : cause trouvée — `runSegment` marquait une pause (`DWELL_MS`, la même que pour tous les points intermédiaires) avant même de démarrer `returnToOverview`, donnant l'impression de 2 actions distinctes (arrivée figée, PUIS une nouvelle transition séparée) plutôt qu'un seul mouvement continu. Pause supprimée spécifiquement pour ce dernier point (gardée entre les points intermédiaires du tour) : `returnToOverview` enchaîne désormais immédiatement, recentrage et dézoom restant par ailleurs déjà pilotés par le même `eased` dans un seul appel `moveCameraTo` par frame (déjà simultanés côté calcul, la pause était le seul vrai problème).
    - Bascule "switch" : contour élargi de 2px (`--nt-border-width-thick`) à 3px (valeur littérale, aucun token existant au-delà) — dernier ajustement, "on est bon" sur la structure à 3 couches.
  - **10e passe retours utilisateur (2026-07-31) — courbe "parfaite" désormais confirmée pour tous les trajets ENTRE 2 points** (retour utilisateur explicite). Restaient 2 trajets pas encore corrigés, tous deux impliquant la vue d'ensemble plutôt que 2 points : `TripDayMapComponent.followFromOverview` (vue d'ensemble -> 1er point) et `GeneralMapCinematicService.returnToOverview` (dernier point -> vue d'ensemble) utilisaient encore le MÊME `eased` (cubic) pour piloter à la fois la position ET le zoom — jamais découplés comme `followScroll`/`computeCinematicZoom` (voir 8e/9e passes). Même correctif appliqué aux 2 : la position garde `eased`, le zoom est désormais piloté par `t` BRUT (lerp direct `overview.zoom <-> focusZoom`, pas de forme parabole ici — ces 2 trajets sont monotones, pas un aller-retour comme un segment point-à-point, seul le DÉCOUPLAGE du rythme comptait).
    - Bascule "switch" : hover retiré sur l'item NON sélectionné, uniquement pour le variant `'subtle'` (le variant `'solid'` garde le sien, règle de base non touchée) — vérifié (fond `rgba(0,0,0,0)` inchangé pendant le survol). Ajustement visuel du `.app-select-button__track` (marge réduite à `0.2rem`, plus proche du bord que le thumb à `0.35rem`) fait directement par l'utilisateur, conservé tel quel.
  - **11e passe retours utilisateur (2026-07-31) — vraie cause racine trouvée par instrumentation** (retour utilisateur : toujours aucun changement perçu sur les 2 trajets "vue d'ensemble", malgré un redémarrage confirmé du serveur — signe qu'il fallait chercher ailleurs qu'un problème de cache). Injection de points géolocalisés connus directement via `TripFacade` (contournant l'autocomplete Google Places, peu fiable en sandbox) + capture d'écran image par image + logs temporaires ont révélé un VRAI bug de course, sans rapport avec la forme de la courbe de zoom :
    - `TripDayMapComponent` a un effect interne, pensé à l'origine pour la vue jour uniquement, qui recentre la caméra sur le 1er point à CHAQUE changement de `points` (via le signal réactif `center`/`zoom` lié au template `<google-map [center]="center()">`) — un canal totalement SÉPARÉ des appels impératifs `moveCamera`/`moveCameraTo` du système cinématique. Un garde-fou (`if (mapHost.currentOwner() === 'general') return`) avait déjà été ajouté en 8e passe, mais restait inefficace : au tout premier passage où `generalMapPoints()` devient non-vide, `#stickyMap` (le conteneur DOM, sous `@if (hasMapPoints())`) pouvait ne pas encore exister — `TripActivitiesComponent` sautait alors l'appel `moveTo` (qui positionne `currentOwner`) tout en laissant `points.set(...)` s'exécuter quand même, sans garde équivalente. `currentOwner` restait donc `null` au moment exact où l'effect interne de `TripDayMapComponent` lisait sa valeur, laissant passer le recentrage parasite AVANT que `attachMap`/`enterTour` n'ait sa chance.
    - Corrigé en gardant l'intégralité du corps de l'effect `points.set` (pas seulement `moveTo`) derrière la même condition `container` — `stickyMap()` étant lu à l'intérieur, l'effect se redéclenche automatiquement dès que l'élément apparaît réellement dans le DOM, garantissant `moveTo` (donc `currentOwner`) toujours posé avant tout `points.set`.
    - Vérifié par capture d'écran image par image (toutes les 500ms) sur un trajet Bruxelles/Paris injecté : le tour se déroule désormais en continu (plus de saut au montage, transition fluide de bout en bout jusqu'à l'arrivée sur le point). Nettoyage : logs de diagnostic temporaires retirés, activités de test supprimées.
  - **12e passe retours utilisateur (2026-07-31)** :
    - **Régression trouvée sur la vue jour** ("plus sur la vue globale en arrivant sur la page") : exactement le MÊME bug de course que la 11e passe, mais dans `DayPanelComponent` cette fois (jamais corrigé là-bas) — `#stickyMap` y est AUSSI sous un `@if` (`activities().length > 0`), et son effect `points.set` était AUSSI séparé de l'effect `moveTo`/`attachMap`. Même correctif appliqué : tout le corps de l'effect `points.set` (dont `moveTo('day')`) regroupé derrière la disponibilité de `container`.
    - Zoom "vue d'ensemble -> 1er point" / "dernier point -> vue d'ensemble" toujours pas satisfaisant après la 11e passe : le VRAI bug (course d'appartenance de caméra, 11e passe) étant maintenant corrigé, le découplage `t` brut vs `eased` introduit en 10e passe pour ces 2 trajets n'avait plus lieu d'être — un lerp de zoom purement linéaire (`t` brut) n'a aucune décélération propre, moins doux que la position et que les segments point-à-point (parabole). Revenu à `eased` pour le zoom aussi dans `TripDayMapComponent.followFromOverview` ET `GeneralMapCinematicService.returnToOverview` — la coordination avec la position n'est plus le problème depuis que la vraie cause (la course) est réglée.
  - **13e passe retours utilisateur (2026-07-31), schémas fournis par l'utilisateur** : la courbe restait insatisfaisante pour "vue d'ensemble -> 1er point" — mauvaise FAMILLE d'easing, pas un problème de découplage. `easeInOutCubic` (symétrique lent-rapide-lent, utilisé jusqu'ici) donne un rythme qui accélère puis ralentit des 2 côtés ; le schéma demandé est asymétrique : quasi immobile juste après avoir quitté la vue d'ensemble, puis accélération nette en approchant du point d'arrivée — un ease-IN, pas un ease-in-out. `TripDayMapComponent.easeInCubic` (`t³`, nouveau, remplace `easeInOutCubic` UNIQUEMENT dans `followFromOverview` — les segments point-à-point gardent leur parabole `4t(1-t)`, confirmée "parfaite") et l'équivalent dans `GeneralMapCinematicService.returnToOverview` (même logique en sens inverse : quasi immobile juste après avoir quitté le point, accélère à l'approche de la vue d'ensemble).
  - **14e passe retours utilisateur (2026-07-31)** : toujours pas bon — demande explicite et précise, cette fois avec la méthode exacte à suivre : réutiliser LITTÉRALEMENT la moitié de la courbe d'un segment point-à-point (confirmée "parfaite"), pas un easing choisi à la main. Un segment (`followScroll`) passe par 3 états notables : au point de départ (t=0), au dézoom maximal (t=0.5, le milieu géographique), au point d'arrivée (t=1) — la vue d'ensemble correspondant exactement à cet état "t=0.5". Dérivation par substitution (`t=0.5+0.5s` pour la 2e moitié, `t=0.5s` pour la 1re), voir les commentaires de code pour le détail :
    - `TripDayMapComponent.followFromOverview` (vue d'ensemble -> point, 2e moitié) : position en ease-OUT cubique `1-(1-s)³` (substitution dans `easeInOutCubic`), zoom en ease-IN quadratique `s²` (substitution dans `4t(1-t)`).
    - `GeneralMapCinematicService.returnToOverview` (point -> vue d'ensemble, 1re moitié, sens inverse) : position en ease-IN cubique `s³`, zoom en `2s-s²`.
    - Ces 2 méthodes n'ont donc plus besoin de leurs propres fonctions d'easing nommées (`easeInCubic` supprimée des 2 fichiers, désormais sans appelant) — les formules, directement dérivées de la parabole/l'ease-in-out des segments, sont assez courtes pour rester inline.
  - **15e passe retours utilisateur (2026-07-31)** — courbes du pool confirmées "parfaites" par l'utilisateur ; restaient une pause manquante et une régression vue jour :
    - Pause (`DWELL_MS`) manquante sur le 1er et le dernier point du tour du pool : `enterTour` enchaînait directement vers le 2e point sans le petit temps d'arrêt déjà présent sur tous les points intermédiaires (ajouté, symétrique) ; `runSegment` avait un cas spécial qui sautait la pause sur le DERNIER point avant `returnToOverview` (retiré, même pause partout).
    - **Régression vue jour confirmée** ("tu as cassé la vue d'ensemble à l'arrivée sur les days") — 2 causes distinctes trouvées par instrumentation (points géolocalisés injectés + patch temporaire de `google.maps.Map.prototype.moveCamera/setCenter/setZoom/panTo` avec horodatage, pas juste des captures d'écran cette fois, pour voir QUI bouge la caméra) :
      1. `GeneralMapCinematicService` (tour du pool) : ses boucles `requestAnimationFrame` (`enterTour`/`runSegment`/`returnToOverview`) ne revérifiaient QUE `generation` à chaque frame, jamais `isActive()` — un tour démarré pendant que l'onglet Général était actif continuait de tourner jusqu'à son terme même après que l'utilisateur ait basculé sur un jour, la caméra PARTAGÉE se faisant détourner en plein vol vers le 1er point du pool (qui, les activités de jour et de pool partageant la même `PoolActivity`, converge visuellement vers UNE activité du jour lui-même — d'où l'impression trompeuse que "le jour" zoomait tout seul). Nouveau `shouldAbort(generation)`, appelé en tête de chaque `step()`/callback de `dwellTimer` : périmé (génération dépassée) -> ne rien faire de plus (déjà géré ailleurs) ; inactif -> repos SANS toucher la caméra (elle appartient désormais à un autre contexte) + reprogrammation du minuteur 2s (même logique que la branche `isActive()` déjà présente dans `onIdleTimeout`, pour permettre une reprise propre au retour sur Général).
      2. `TripDayMapComponent` avait un recentrage par défaut vestigial sur le 1er point (`center.set(...)`, lié en réactif au template `[center]="center()"`) qui ne s'appliquait qu'à la vue jour depuis la 11e passe (le pool en était déjà exempté) — pensé à l'origine comme centre par défaut avant tout scroll, il gagnait désormais la course contre `DayScrollSyncService.updateMapFromScroll`, exactement le même anti-pattern déjà écarté pour le pool. Les 2 contextes ayant maintenant leur propre système établissant la caméra correcte à l'activation, ce recentrage par défaut est retiré entièrement (plus aucun appelant de `center.set` après le tout premier montage). Effet de bord découvert dans la foulée : `DayScrollSyncService.attachMap` avait un correctif de repaint post-transfert DOM (`nativeMap.setCenter(map.center())`) qui lisait ce même signal désormais figé sur sa valeur initiale (Paris, jamais réécrite) — corrigé pour relire `nativeMap.getCenter()` (sa propre position actuelle) plutôt que le signal, un simple "nudge" de repaint ne doit jamais repositionner la caméra.
    - Vérifié par un scénario Playwright dédié (activités géolocalisées injectées sur un jour + logs caméra horodatés + captures d'écran à intervalles croissants, jusqu'à ~9s après arrivée) : plus aucune dérive dans le temps, la vue d'ensemble reste stable — fichier de test temporaire supprimé après vérification, comme d'usage.